# aihub#411 — design-decision adjudication table (tiers 1–2)

**Read-only.** No production code, schema, test or gate was touched.

Every behaviour cell carries a **semantic anchor** — file plus the enclosing symbol — resolved
against `origin/main` **`1ec3bdc`** (`1ec3bdc62259f2675f263bfd880b1a487e6b1b91`, = aihub#402's
merge), or a command anyone can re-run. 🔴 **Not a `file:line`, and that is this repo's rule
rather than a preference**: check C1 of `scripts/pf_docs_contract_check.py` forbids line
citations anywhere in `docs/` and CI enforces it on `docs/**`. See **T1-13** — it is a row in
this table, it is the policy this document's own first push violated, and its evidence is that
18 of 23 line citations elsewhere in `docs/` already pointed at the wrong line. Every anchor
below was resolved from a re-measured line and then verified to be the declaration whose span
*contains* that line (222 symbols, 0 failures); the aihub#385-era numbers are stale and were
not reused.

The deliverable is a **recommendation**, not a decision. Nothing below is adjudicated.
The owner rules per row; accepted rows become contract-card lines and the repo policy list.

---

## 0. How to read this, and what the numbers are

**Row count: 35 of a hard cap of 40** — tier 1 = 13, tier 2 = 22. Ten further candidates are
parked by name in §4 with the reason each was cut. Single-tool residue is out of scope per the wi.

**Ordering.** Rows where the repo contradicts *itself* come first (T1-1 … T1-9, T2-1 … T2-9),
then the rest, each block ordered by blast radius = *tools affected × measured call volume*.
⚠️ Blast radius is not severity. **T2-8 (`roleLevel`) is the most severe defect in this
document and sits eighth in its block**, because it reaches two endpoints. Do not read
position as priority.

**Status vocabulary used in the "Decided?" column.**

| tag | meaning |
|---|---|
| `LANDED` | decision made *and* on `origin/main`, with a gate |
| `LANDING` | decision recorded in a wi's `owner_decision_2026_09_07`; wi is `running` right now — **do not re-open** |
| `DECIDED, QUEUED` | decision recorded in attrs; wi is `queued`, nobody is executing it |
| `NO DECISION EVIDENCE FOUND` | searched wi attrs, code comments, CHECKs and commit messages; found none. **This is a finding.** |

### 0.1 Measurements taken for this wi (2026-09-07, re-runnable)

| what | value | how |
|---|---|---|
| published tools | **50** | `polyforge dump-mcp-schemas` (plugin `1.1.23` binary) |
| published top-level params | **240** | same dump, summed over `tools[*].params` |
| DB `CHECK` constraints | **44** | `grep -rhoiE 'CHECK *\(' internal/db/migrations/*.sql \| wc -l` |
| error codes | **50** | `grep -c 'ErrCode = "' internal/domain/errors.go` |
| `pf_*` calls, 21-day window | **2,992** across **78** transcript files | `type=="tool_use"` blocks in `~/.claude/projects/-root-code-aicoding-gmi-ws/*.jsonl`, files with `mtime` ≥ now−21d, name suffix `pf_*` |
| published tools with **zero** calls in that window | **12 of 50** | set difference of the two above |
| local gate on `1ec3bdc` | `go build ./...` = 0, `go vet ./...` = 0, `go test ./... -count=1` = 15 pkg `ok`, **0 FAIL** | isolated `GOCACHE`, `GOWORK=off` |

**Per-tool call volume (21-day window, 2,992 total).** Used for every blast-radius claim below.

`pf_update_work_item` 354 · `pf_update_step` 281 · `pf_create_work_item` 275 ·
`pf_get_work_item` 255 · `pf_claim_work_item` 230 · `pf_complete_attempt` 185 ·
`pf_recall` 173 · `pf_push` 153 · `pf_pr` 140 · `pf_emit_event` 118 · `pf_save_artifact` 93 ·
`pf_remember` 87 · `pf_get_step` 82 · `pf_list_work_items` 77 · `pf_commit` 74 ·
`pf_predict_conflicts` 66 · `pf_acquire_locks` 61 · `pf_cancel_work_item` 48 · `pf_wrap` 46 ·
`pf_list_dependencies` 41 · `pf_pause_attempt` 29 · `pf_get_ready_queue` 29 ·
`pf_read_events` 26 · `pf_activate_memory` 21 · `pf_batch_create_work_items` 11 ·
`pf_create_dependency` 8 · `pf_update_project` 8 · `pf_get_memory` 7 · then 1–2 each for
`pf_list_projects`, `pf_remove_dependency`, `pf_list_users`, `pf_revoke_api_key`,
`pf_update_memory`, `pf_ship`, `pf_reinforce_memory`, `pf_diff`, `pf_create_user`,
`pf_force_takeover`.

**Zero calls in the window (12):** `pf_adopt_artifact`, `pf_close_artifact`,
`pf_ignore_artifact`, `pf_cut_alpha`, `pf_promote`, `pf_resolve_commit`, `pf_redact_memory`,
`pf_rotate_identifier`, `pf_create_project`, `pf_create_api_key`, `pf_update_user`,
`pf_whoami`.

### 0.2 🔴 Numbers borrowed from other wi's attrs — different sampling frames, do not combine

| number | source | frame |
|---|---|---|
| 12,133 `pf_*` calls; **301 (2.5%)** carry ≥1 unpublished param; `pf_update_step.expected_version` **202 of ~1,120 (18%)**; `pf_complete_attempt.summary` 24; `pf_list_work_items.brief` 22 | `aihub#389.attrs.owner_decision_2026_09_07.measured_risk_of_hard_reject` | "transcript corpus, last 21 days, files > 100k" |
| **118 of 698 (~17%)** `completed` step calls have no prior `in_progress` for that (wi, step) in the same transcript — an **upper** bound | `aihub#398.attrs.owner_decision_2026_09_07.decision` | last 21 days of transcripts |
| **+1,317 B** of always-resident schema for two description edits (`pf_claim_work_item` 1,445 → 2,429 B; `pf_force_takeover` 262 → 595 B) | `aihub#392.attrs.execution_notes_2026_09_07.resident_schema_cost` | real `tools/list` on both trees |
| locks held at claim 2/2/0/0 vs files actually changed 5/9/20/4 over four consecutive wi's | `internal/domain/commit_locks.go` (`FnReconcileCommitLocks`) (aihub#366) | 2026-09-05/06 |

⚠️ **§0.2's 12,133 and §0.1's 2,992 are not the same population** (`files > 100k` vs all files,
and a different block filter). Dividing one into the other produces a fake percentage. Each
number is cited only against its own denominator, and where a row needs a percentage this
document has none, the cell says **not measured**.

⚠️ `polyforge dump-mcp-schemas` emits **tool** descriptions but **no param descriptions**
(verified: 240 params, 0 with a `description` key). It is therefore **not** a wire-size
instrument, and no byte figure in this document comes from it.

---

## 1. Tier 1 — repo-wide policies (13 rows)

Rows T1-1 … T1-9 are self-contradicting. T1-10 … T1-13 are consistent-but-undecided or
consistent-and-decided.

| # | Policy | Current behaviour on `1ec3bdc` (with location) | Decided? (evidence) | Places doing it DIFFERENTLY | What callers actually send | Recommended ruling + one-line rationale |
|---|---|---|---|---|---|---|
| **T1-1** | **Unknown top-level params** | `internal/mcp/tools_lifecycle.go` (`objectSchema`) emits `{"type":"object","properties":…}` and never `additionalProperties:false`; the untyped `AddTool` form does no schema validation; echo's `c.Bind` drops untagged keys silently. A misspelled param returns 200. Own code admits it: `tools_lifecycle.go` (`registerLifecycleTools`). | **DECIDED, QUEUED** — `aihub#389.attrs.owner_decision_2026_09_07`: two phases, phase 1 = report extras under the existing `request_adjusted` convention + fix the top offenders; phase 2 = `additionalProperties:false` only after a 14-day re-measure shows <0.1%. 🔴 **Neither phase has landed** — `#389` is `status:queued`, `current_attempt_id:null`. My brief said "phase 1 landed or landing"; that is **false**. | 🔴 The *same endpoint* rejects unhonourable args loudly one layer down: `validateNextStepArgs` (`internal/server/routes_step.go` (`handleUpdateStep`), called before anything acts) refuses every `next_step`/`heartbeat` combination it cannot honour, and its comment gives the reason — "never accepting a parameter we are not going to act on" (`routes_step.go` (`handleUpdateStep`)). The schema layer does the opposite for every tool. | §0.2: 2.5% of 12,133 calls carry ≥1 unpublished param; `expected_version` on 18% of `pf_update_step`. `aihub#389.attrs.source_of_offenders_2026_09_07` measured that **no skill or fragment text teaches `expected_version`** — it is model habit, so no prose edit can remove it. | **Ship phase 1 as decided, and put the phase-2 trigger in code, not in prose.** The 18% figure is the whole argument for report-before-reject; a threshold that lives only in a wi's attrs will not be re-measured. |
| **T1-2** | **Out-of-range vs malformed numerics** | Two rules, written down once: **Rule 1 parse-or-reject → 400** (`internal/server/queryparam.go` (`queryInt`); `queryFloat`; `queryFloatInRange`) and **Rule 2 clamp-to-ceiling + disclose** via `request_adjusted` (`internal/domain/request_adjusted.go`). The dividing question is stated at `queryparam.go` (`queryInt`): whose mistake is it. Enforced by an AST gate over every non-test file in `internal/server` (`queryparam_gate_test.go`). | **LANDED** — `aihub#255` / `#267` / `#340`; the policy text is `queryparam.go` (`queryInt`), the gate is `queryparam_gate_test.go`. | 🔴 **Three escapes, all outside the gate's package.** (a) `GetReadyQueue` clamps `max>200` **without disclosing** — `internal/domain/work_items.go` (`GetReadyQueue`), and the code says so itself in that function's own clamp comment ("obeys half of Rule 2 … `max=5000` and `max=200` return byte-identical responses"). (b) `internal/mcp/tools_memory.go` (`recallNumArg`) returns **0** for unparseable text at hop 2, and 0 is `similarity_threshold`'s OFF value — Rule 1's original defect, one hop upstream of the fix. (c) `cursor` never reaches a conversion, so the gate cannot see it (see T1-6). | `pf_get_ready_queue` 29, `pf_recall` 173 calls (§0.1). Share of those carrying an out-of-range numeric: **not measured**. | **Extend Rule 1 to hop 2 and Rule 2's disclosure to `ReadyQueue`.** The policy is right and the gate is real; what is missing is that the gate is package-scoped while the policy is repo-scoped — so widen the gate, not the policy. |
| **T1-3** | **`base_strength`: published range vs enforced range** | 🔴 Three disjoint answers for one field. Schema publishes **`(0-1)`** (`internal/mcp/tools_memory.go` (`rememberSchema`)). DB CHECK is **`BETWEEN 1 AND 5`** (`internal/db/migrations/0006_events_memories.sql` (`memories.base_strength`)). Go default is **`3.0`** (`internal/domain/memory.go` (`Remember`)) — outside the published range. `memory.go` (`Remember`) validates the type but **never the strength**, so a schema-legal `0.5` reaches the CHECK and returns **500** via `internal/domain/pgx_err.go` (`dbErrCause`). | **NO DECISION EVIDENCE FOUND.** No wi, no `owner_decision`, no comment, no commit message names this. The CHECK is from migration 0006 (pre-audit); the `(0-1)` string has no citation attached. | 🔴 **The sibling write path clamps.** `pf_reinforce_memory`'s `strength_delta` is clamped into `[1,5]` **in Go** at `internal/server/routes_memory.go` (`handleReinforceMemory`), and `routes_memory.go` (`handleReinforceMemory`) documents that as the contract. Same column, two policies. | `pf_remember` 87, `pf_update_memory` 1, `pf_reinforce_memory` 1 (§0.1). How many pass `base_strength` at all: **not measured**. | **Publish `1-5` and validate it in Go at the `Remember` entry point, reusing the reinforce clamp's bounds.** A published range disjoint from the enforced one is worse than no range: it *invites* the 500. |
| **T1-4** | **DB CHECK violation → 500 vs Go-level 400** | Constraint-only enforcement returns **500 INTERNAL_ERROR with the raw SQLSTATE**, because every DB error path funnels through `dbErr`/`dbErrCause`/`pgxErr` (`internal/domain/pgx_err.go` (`dbErr`, `dbErrCause`, `pgxErr`)) and only class 40 is reclassified. Unvalidated in Go on `work_items`: `priority` (CHECK `0002_work_items.sql` (`work_items.priority`)), `source` (`work_items.source`), `labels ≤ 20` (`work_items.labels`), `content ≤ 20000` (`0011_wi_content.sql` (`work_items.content`)). Verified absent: `grep -n 'len(req.Labels)' internal/domain/*.go` → 0 hits; no `"urgent"` literal in any non-test validator. | **DECIDED, QUEUED** — `aihub#396.attrs.owner_decision_2026_09_07` states it as a **repo-wide policy row**: *"A vocabulary or limit that a DB CHECK enforces is ALSO validated in Go and answered 400 naming the field; the CHECK is the last line of defence, never the caller-facing one."* `#396` is `status:queued`, `current_attempt_id:null` — my brief said "in flight"; that is **false**. | 🔴 **The policy already has three implementations and they were never generalised.** `internal/domain/declared_resources.go` (`ValidateRequestedLocks`) exists *specifically* because the CHECK produced a misleading 500 — its doc comment quotes the 23514 text. `ValidateDeclaredResources` does the same for declared types. `artifact_summary`'s 4096 cap is rejected before the write (`internal/server/routes_step.go` (`handleUpdateStep`), `validateTerminalStepArgs`, aihub#390). 🔴 **And one number, 20000, gets all three treatments at once:** `work_items.content` is CHECK-only → 500 (`0011_wi_content.sql` (`work_items.content`)); a memory **commit reply** body is capped in Go with a proper **413 `PAYLOAD_TOO_LARGE`** (`internal/domain/memory.go` (`ReplyCommit`)); and `memories.content` itself has **neither** — no CHECK (`0006_events_memories.sql` (`memories.content`) is a bare `TEXT NOT NULL`) and no Go cap, so the reply cap's own comment ("matching the memory body cap searched in domain code", (`trg_mem_immortal.FOR`)) names a cap that does not exist. A **units** mismatch rides along: the CHECK's `length()` counts CHARACTERS while the `content_len` the MCP echoes is BYTES — "`content_len` is not the number to compare against that 20000" (`internal/mcp/wi_echo_slim.go` (`dropContentEcho`)). | `pf_create_work_item` 275 + `pf_update_work_item` 354 + `pf_remember` 87 + `pf_update_step` 281 ≈ 997 calls in scope (§0.1). Rate of bad enum values: **not measured**. | **Adopt `#396`'s policy row verbatim as repo policy and give it a gate that enumerates CHECKs, not fields.** Three hand-written instances over three wi's is the signature of a class nobody closed; a per-field fix produces a fourth — and the 20000 split shows the class is already wider than the fields `#396` names. |
| **T1-5** | **Response projection: keep-list vs delete-list** | Four projections. **Delete-list** (forward by default): `list_wi_slim.go` (`slimListWorkItemsResult`), `claim_response_slim.go` (`slimClaimResult`), `wi_echo_slim.go`. **Keep-list** (drop by default): `internal/mcp/recall_slim.go` (`slimRecallResultMode`) — `keep := map[string]bool{…}`, and its own INVARIANT comment above the `keep` map calls the whitelist "opt-in", the `unmatched_types` copy names it *"third instance of this whitelist swallowing a new field"*, and the `request_adjusted` copy lists the casualties: `total` (aihub#249), the truncation pair (#269), `unmatched_types` (#289). | **LANDED for three of four** — `aihub#281` built `list_wi_slim` as a delete-list explicitly "so it cannot become the fourth instance"; `aihub#388.attrs.execution_notes_2026_09_07` converted the claim response and reports the mechanism gate (`claim_response_projection_test.go` (`TestClaimResultCarriesEveryClaimResponseField`), reflection over `domain.ClaimResponse`). 🔴 **`recall_slim` was deliberately left**: `#388`'s own `findings_not_fixed` says it "is STILL a keep-list and is instances 1-3 of this same pattern … folded rather than filed" — **so no wi exists for it.** | `recall_slim.go` is the last keep-list, on the 7th-most-called tool. Its cost is already itemised in `request_adjusted.go` (`RequestAdjustment`): disclosing a clamp needed a cross-package edit *because of that whitelist*, while saying nothing cost zero — and `aihub#309` deleted a clamp rather than pay it. | `pf_recall` 173 calls (§0.1); `pf_claim_work_item` 230, `pf_list_work_items` 77, `pf_get_work_item` 255 on the delete-list side. | **Convert `recall_slim` to a delete-list and file the wi `#388` folded.** The repo has already paid for this pattern four times and written down why; the only thing missing is a work item, and a finding folded into a wrapped wi is a finding nobody will execute. |
| **T1-6** | **Bad cursor / execution-time query error** | An error, not an empty page: every list loop now checks `rows.Err()` (`internal/domain/work_items.go` (`checkDedup`), (`listWorkItemsPage`), and 9 sites across `conflicts.go` / `dependencies.go`) because pgx defers execute-time failures there. 🔴 **But the status is 500 with the driver's text**: a `cursor` that will not cast at `$n::timestamptz` is a *caller* error routed through `pgx_err.go` (`dbErrCause`). The code names exactly this input at `work_items.go` (`listWorkItemsPage`). `cursor` is read raw (`internal/server/routes_memory.go` (`handleRecall`) `c.QueryParam("cursor")`) and never validated. | **HALF LANDED.** `aihub#382`/`#386` decided *error-not-empty-page* and gated the class (`internal/citest/rowserr/`). **NO DECISION EVIDENCE FOUND** for the status code: nothing says a malformed cursor should be 400. | 🔴 Directly against T1-2 Rule 1, which exists to stop exactly this ("`?limit=12abc` was read as 12 — a page size the caller never wrote"). The Rule-1 gate cannot catch it: `queryparam_gate_test.go` follows a request value into a **conversion or a comparison**, and a string passed through to SQL is neither. The gate's own doc names its blind spots (`queryparam.go` (`queryInt`)) — this is a fourth. | `pf_list_work_items` 77 + `pf_recall` 173 + `pf_read_events` 26 = 276 calls carry a cursor-capable surface (§0.1). Malformed-cursor rate: **not measured** (a cursor is server-minted, so callers rarely hand-write one). | **Validate `cursor` as RFC3339Nano at the handler and 400 on failure — the same shape as `queryInt`.** A 500 on a caller-supplied token sends the reader to the server logs, which is the one place the fix is not. |
| **T1-7** | **Idempotency replay semantics** | 🔴 **Two mechanisms, one word.** (a) HTTP: `Idempotency-Key` header → in-process, bounded, non-durable cache keyed `<api_key_id>:<idempotency_key>`, replays the cached body on a matching fingerprint (method+target+body) and answers **409 `IDEMPOTENCY_KEY_REUSED`** on a mismatch (`internal/server/idempotency.go` (`cachedResponse`, `IdempotencyMiddleware`); mounted on **all** of `/v1` at `internal/server/router.go` (`NewRouter`)). (b) Claim: `idempotency_key` **body** param → DB dedup on `run_attempts.idempotency_key`, returns the **existing attempt**; the MCP layer now reuses the recorded secret (`internal/mcp/tools_lifecycle.go` (`registerLifecycleTools`) `recordedClaimSecret`). | **LANDED for (b)** — `aihub#392.attrs.merged_2026_09_07` (merge `51108c9`), with a DB-gated three-arm test. **LANDED for (a)'s design** — `aihub#152`, and `internal/domain/errors.go` (`ErrIdempotencyKeyReused`) records why a reused key cannot replay. **NO DECISION EVIDENCE FOUND** that (a) and (b) are meant to be different things, or that (a) has a client. | 🔴 **Mechanism (a) is a MANDATORY protocol element that no client sends.** The design doc requires it on every mutating request — *"Mutating 操作：`Idempotency-Key: <client-generated idem_<8b62>>`（所有 POST/PATCH）"* (`docs/design/polyforge-v1-design.md` §4.1 *认证*), restated as `H-R3-8` *"仅走 HTTP header（所有 POST/PATCH 必须带）"* (`H-R3-8`), and named as required for force_takeover specifically (the `force_takeover` endpoint row, `H8`). Measured on the live Go tree: `git grep -Iin "idempotency-key"` finds it **only** in `internal/server/idempotency.go`, `internal/domain/errors.go` (`ErrIdempotencyKeyReused`), `internal/server/idempotency_test.go` and the design doc — **no hit in `pkg/client`, `internal/mcp` or `internal/cli`**. So the middleware is exercised by its own unit tests and by nothing else, and the "all POST/PATCH must carry it" contract has a compliance rate of **zero**. (Scope of that negative: the live Go tree. The archived `v0/` Python tree holds `v0/tests/v3/wire_multi_agent/test_idempotency_dedup.py`, which is not a producer against this server.) | `pf_claim_work_item` 230 calls, every one carrying a body `idempotency_key` (`pf-work` mints a fresh ULID per call — see `#392.attrs…findings_not_fixed`). Header path: **0**. | **Rename in prose, and decide whether (a) keeps existing.** Either give the client the header or retire the middleware — but the row the owner must rule on is that "idempotency" currently names two unrelated guarantees, so a caller reasoning about one gets the other's semantics. |
| **T1-8** | **`intent:"read"` scope** | Honoured for **`file_scope` only**: `internal/domain/conflicts.go` (`derivedLock`) drops the lock iff `lockType == "file_scope" && res.Intent == "read"` (the condition is in `derivedLock` itself, not in `resourceToLock`). `repo` → `git_branch` and `service` → `deploy_env` always lock (`resourceToLock`). The restriction is written as a condition on `lockType`, not on `res.Type`, "so that widening it cannot happen by accident" (`derivedLock`). | **LANDED for the mechanism** — `aihub#342` unified four derivation sites through `derivedLock`; before it, `{"type":"path","intent":"read"}` took a write lock at claim while `pf_predict_conflicts` called the same input `info` ("Two tools, one input, opposite answers", `derivedLock`). **DECIDED, QUEUED for the contract text** — `aihub#395.attrs.owner_decision_2026_09_07` part (1): state that `read` is honoured for `path` entries only. `#395` is `status:queued`. | 🔴 The published contract is still unqualified: `declaredResourcesProp` promises `read` "takes no write lock" for **all** types and is advertised on `pf_predict_conflicts` / `pf_update_work_item` / `pf_create_work_item` / `pf_batch_create_work_items`. And the code itself says the repo/service question is **open**: "Whether `intent=read` should mean anything at all for repo/service is genuinely undecided; deciding it needs rule 2 changed in the same breath" (`conflicts.go` (`derivedLock`)). | `pf_create_work_item` 275 + `pf_update_work_item` 354 + `pf_claim_work_item` 230 + `pf_predict_conflicts` 66 + `pf_batch_create_work_items` 11 = 936 calls publish this param (§0.1). Share declaring `intent:"read"`: **not measured**. | **Take `#395` part (1) as written — narrow the prose, not the behaviour — and file the repo/service question as its own wi with `PredictConflicts` rule 2 in the same scope.** The code has already named the coupling; splitting it recreates the rule-1-vs-rule-3 contradiction `#342` removed. |
| **T1-9** | **Stale hop-1 prose: withdraw, fix, or document?** | 🔴 **Four different dispositions for one class, all in the same 24 hours.** *Withdraw the param*: `#387` deleted `non_conflicting` from the schema rather than implement it; `#394` will do the same to `mode`; `#395` part (2) to `base_branch`. *Fix the code so the prose becomes true*: `#388` widened the claim projection, and its `execution_notes` note (3) records that `post-claim-routing.md` (the "read wi_type from the claim response (preferred)" line) was **not** edited because the fix made it true — "the reverse of stale". *Cancel as description-only*: `#397`, `#400`, `#401` were all `status:cancelled` on 2026-09-07 with their measured findings intact in `content`. *File as a bug*: `#408` (queued) for two prose sites that survived `#392`. | **PARTLY DECIDED.** Plan B ("withdraw from the schema, do not implement") is recorded in `#387.attrs.owner_decision_2026_09_07` and re-applied in `#394`/`#395`. **NO DECISION EVIDENCE FOUND** for the cancel-vs-file split: `#397`/`#400`/`#401` carry no `owner_decision` and no cancellation reason. | 🔴 The live instance: `internal/mcp/tools_lifecycle.go` (`registerLifecycleTools`) still says the handler "mints a fresh session_secret on every call" and the **`RECOVERY:` clause** of its state-file-write error still tells the caller "call `pf_claim_work_item` again with a **NEW** idempotency_key — replaying the same key … leav[es] every later call unauthorized". Both are false since `#392` (`recordedClaimSecret`, `registerLifecycleTools`). An agent obeying that clause performs a needless takeover. Tracked as `#408`, queued. | Every one of the 50 tools' descriptions is in the prefix of every request; hop-1 prose is what an LLM caller sees and the only thing it sees. Volume: all 2,992 calls (§0.1). | **Make the disposition a rule, not a judgement call: prose that contradicts hop 3/4 is a bug with the same priority as the behaviour, and "description-only" is not a cancellation reason.** `#397`/`#400`/`#401` were cancelled with measured, uncontested findings in their bodies; those findings are now only reachable by knowing the wi numbers. |
| **T1-10** | **Schema wire-size budget** | Exactly **one** of 50 published tools has a ceiling: `listWorkItemsSchemaBudget = 5400` B (`internal/mcp/tools_list_wi_schema_size_test.go` (`listWorkItemsSchemaBudget`)), with a 1,000 B floor so the ceiling cannot pass vacuously (`TestListWorkItemsSchemaStaysWithinItsWireBudget`) and an instruction not to just raise the number (`listWorkItemsSchemaBudget`). | **LANDED for one tool** — `aihub#276`/`#277`, cited in that file's own header. **NO DECISION EVIDENCE FOUND** for the other 49. | 🔴 `#392.attrs…resident_schema_cost` is the proof it matters: two description edits cost **+1,317 B always-resident** (after review trimmed a first draft's +1,945 B), and the note ends "the size guard that exists is scoped to `pf_list_work_items`, so **nothing would have flagged this**". | All 2,992 calls pay the whole schema prefix (§0.1). Byte totals per tool: **not measured** — `dump-mcp-schemas` omits param descriptions (§0.1), so the only honest instrument is a Go test through the real `tools/list`, which this read-only wi may not add. | **Replace the per-tool constant with one budget over the whole `tools/list` payload, plus a per-tool ceiling derived from it.** A ceiling on one tool prices the cheapest change and leaves the other 49 free; the resident cost is a property of the payload, not of a tool. |
| **T1-11** | **Ratchet-file locking convention** | `internal/citest/dbtestcov/gated_tests.txt` and `.github/workflows/ci.yml` are shared append-only ratchets: the measured inventory must **equal** the manifest exactly, so an entry the inventory lost and one nobody wrote down both fail (`internal/citest/dbtestcov/main.go` (`checkManifest`), (`main`), (`checkManifest`)). An empty `-gated-set` is refused rather than silently disabling the ratchet (`main`). Locks on those two files are **not** pre-declared; `pf_commit`/`pf_ship` acquire a `file_scope` lock for every path in the commit that the attempt does not already hold, and hold it until the attempt ends (`internal/domain/commit_locks.go` (`FnReconcileCommitLocks`), and the `pf_ship` tool description). | **LANDED** — `aihub#320` (set-not-number, `main.go` (`main`)) and `aihub#366` (commit-time gate, `commit_locks.go` (`FnReconcileCommitLocks`), which records the owner's 2026-09-06 decision **and both rejected alternatives**: warn-only, and hard-block-with-manual-fix). | Two neighbours hit this in-flight and both handled it by hand: `#392.attrs…rebase_onto_a_neighbour_sharing_the_ratchet_files` (stash round-trip, deliberately leaving the commit to `pf_ship` so the lock gate would still **run** rather than report `not_run`) and `#399` (211 manifest entries, 3 new). Auto-merge succeeded with no conflict both times — "precisely where an interleaved CI step goes unnoticed". | `pf_ship` 1, `pf_commit` 74, `pf_acquire_locks` 61 in the window (§0.1). | **Write the convention down where an executor will read it — the two ratchet files' own headers — and keep the commit-time gate as the enforcer.** The mechanism is sound; what is undocumented is the *operating rule* ("never pre-declare a ratchet; let the commit gate take it"), which two executors independently had to re-derive. |
| **T1-12** | **`request_adjusted` as the one disclosure convention** | One generic `[{param, requested, applied}]` list, appended to by every clamp, **absent** (not `[]`) when nothing was adjusted (`internal/domain/request_adjusted.go` (`RequestAdjustment`)). The reasoning is explicit: a per-clamp field would reproduce the whitelist problem once per clamp (`RequestAdjustment`); no opt-in flag, because `aihub#279` measured that a param on a low-frequency tool can be net −1.7× (`RequestAdjustment`); and the boundary is permanent — it reports only what the server **did to a value it held**, never a param that never arrived (`RequestAdjustment`). | **LANDED** — `aihub#314`, with three tiers of test (`internal/domain/request_adjusted_test.go` on the bytes, `internal/mcp/request_adjusted_wiring_test.go` to the model, `internal/mcp/request_adjusted_e2e_db_test.go` against a real server). | 🔴 One documented exemption remains and is self-declared: `ReadyQueue` has no such field, so `GetReadyQueue`'s clamp cannot disclose (`internal/domain/work_items.go` (`GetReadyQueue`)) — see T1-2(a). Stated as a known gap rather than fixed, on the ground that a half-done version would be no better. | `pf_list_work_items` 77 (the one live producer, for `limit`), `pf_get_ready_queue` 29 (the exempt consumer) (§0.1). | **Keep the convention exactly as decided, and close the one exemption by adding the field to `ReadyQueue`.** The absent-key shape carries a cost the file already names — absence means both "nothing adjusted" and "old server" — which is acceptable only while the field asserts nothing; do not let it grow a meaning. |
| **T1-13** | **How docs cite code** | 🔴 **A line-number citation in `docs/` is FORBIDDEN and gated.** `scripts/pf_docs_contract_check.py` check **C1** rejects any `<file>.<go\|sql\|py\|ts\|tsx\|js\|sh\|yml\|yaml>:<line>` token anywhere under `docs/`, and any filename-less backticked *range* (`` `:N-M` ``). The required form is a **semantic anchor** — file plus symbol — which check **C2** then verifies by existence. Wired into `.github/workflows/contract-lint.yml` on `docs/**`, precisely so "a docs-only edit that … reintroduces a line-number citation, matches NO glob above and would otherwise never be checked. An absent check reports green." | **LANDED, with a measurement** — `aihub#352`. The number that decided it is in C1's own header: *"Measured on 2026-09-03 against `e8fbfcb`: of the 23 real line-number citations in `docs/superpowers/`, **18 pointed at the wrong line** and 3 more were off by one or two."* The escape hatch is one `C1_ALLOWED` map **inside the gate's own file**, with a written rule against using it: *"Do not add an entry to make a real citation pass — replace the citation with a semantic anchor instead, which is the whole point of C1."* | 🔴 Two self-declared gaps. (a) The extension set is **source-only**, so `.md:<line>` is *not* gated — yet `docs/design/polyforge-v1-design.md` is 4,620 lines and, in the gate's own words, "208 KB and actively edited", so a citation into it rots exactly as silently. (b) Only the **range** form of the bare anchor is caught: a single `` `:563` `` is indistinguishable from a port (`docs/deployment.md` has five real ones), so C1 states outright that "Reintroducing a filename-less SINGLE line anchor is therefore NOT gated." Both are honest, both are open. | 🔴 **This document was the counter-example.** Its first push carried **156 C1 errors** and turned Contract Lint red on a doc-only PR — because this wi's own executor brief mandated `file:line` on every behaviour cell, the opposite of the shipped policy. All 222 citations were converted to semantic anchors; §5 records the episode. | **Nothing to re-decide — but propagate it.** Add `md` to C1's extension set (with an allowlist entry for the design doc's own DDL/changelog line references if needed), and state the citation form in the executor-facing docs: an instruction to use `file:line` is currently reachable by anyone briefing a documentation task, and the gate is the only thing that says otherwise. |

---

## 2. Tier 2 — cross-tool nouns (22 rows)

Rows T2-1 … T2-9 are self-contradicting.

| # | Noun | Current behaviour on `1ec3bdc` (with location) | Decided? (evidence) | Places doing it DIFFERENTLY | What callers actually send | Recommended ruling + one-line rationale |
|---|---|---|---|---|---|---|
| **T2-1** | **`work_items.status` — vocabulary vs which fields a status may edit** | Vocabulary is 7, CHECK-enforced: `queued, running, paused, blocked, wrapped, failed, cancelled` (`internal/db/migrations/0002_work_items.sql` (`work_items.status`)). 🔴 **Editability is per-field, with three different status sets and three different error codes.** `goal`: `queued\|paused` only → **400 `GOAL_CHANGE_NOT_ALLOWED`** (`internal/domain/work_items.go` (`UpdateWorkItem`)). `wi_type`: `queued\|paused` only → **403 `WI_RECLASSIFY_FORBIDDEN`** (`UpdateWorkItem`). `content`: `queued\|paused\|running\|blocked` → **409 `CONFLICT_TERMINAL_STATE`** (`UpdateWorkItem`). `attrs`, `labels`, `priority`, `milestone`, `declared_resources`: **no status guard at all** — writable on a wrapped wi. | **PARTLY DECIDED.** `internal/domain/work_items.go` (`cancelGate`) is a real decision with its reasoning attached (`aihub#242`: state before permission, "409 means wrong state, 403 means wrong caller, and the two are no longer conflated"). The transition graph is in `docs/design/polyforge-v1-design.md` §3.1 *work_items.status*. **NO DECISION EVIDENCE FOUND** for the per-field split, or for why `goal` and `wi_type` share one permission predicate and answer different HTTP statuses. | 🔴 `goal` and `wi_type` use the *identical* predicate `isReporter \|\| projectRole=="maintainer" \|\| callerRole=="admin"` (both inside `UpdateWorkItem`) and return 400 vs 403 for the same denial. And `#242`'s own principle — state ≠ permission — is violated by both: a terminal-state `goal` edit is reported as a *permission* failure. | `pf_update_work_item` **354 calls — the single most-called tool** (§0.1); `pf_cancel_work_item` 48. Which fields those calls carry: **not measured**. | **One editability matrix for the whole struct, one error code per rejection *kind* (409 for state, 403 for permission), and no field silently exempt.** `#242` already made this distinction for cancel; the same reasoning applies to update and was never carried across. |
| **T2-2** | **Step model: `current_step` / `current_step_status` / `step_attempt_id` / `completed_steps`** | `wi_step_state` holds `current_step` (nullable), `current_step_status ∈ {idle, in_progress}` (CHECK `0005_step_state.sql` (`wi_step_state.current_step_status`)), `current_step_attempt`, `step_started_at`, `version`. History is append-only in `wi_step_completions` with `status ∈ {completed, failed}` (`wi_step_completions.status`) and `artifact_summary ≤ 4096` (`wi_step_completions.run_attempt_id`). 🔴 A `completed`/`failed` UPDATE has **no** idle-or-in_progress predicate and files the row under the **stored** `current_step`, not the request's `step_id`. | **LANDING** — `aihub#398` is `status:running`, `attrs.owner_decision_2026_09_07`: **two steps, only step 1 in scope.** Step 1 = *identity* predicate (request `step_id` must equal stored `current_step`; mismatch = 409 naming both). Step 2 = *state* predicate (`current_step_status='in_progress'`) is **explicitly deferred** on measurement. 🔴 **Do not re-open.** | 🔴 The design doc has promised the missing guard since v1.15: "server 校验 `wi_step_state.current_step_attempt == $x` … 后到者 409 `CONFLICT_STEP_ATTEMPT_MISMATCH`" (`docs/design/polyforge-v1-design.md` §3.3 *wi_step_state*). The error code exists (`internal/domain/errors.go` (`ErrConflictStepAttemptMismatch`)). The predicate did not. `pf_update_step`'s own description says "concurrency is guarded server-side by the idle-step predicate" — `#398`'s decision includes rewriting that sentence to say what **is** guarded after step 1. | `pf_update_step` **281 calls** (§0.1). §0.2: **118 of 698 (~17%, upper bound)** `completed` calls had no prior `in_progress` — which is exactly why step 2 is deferred rather than shipped. | **Accept `#398` as scoped and hold step 2 behind a corpus re-measure, as decided.** The 17% is the whole argument: a state predicate today would 409 real `pf-execute` flows, so the sequencing *is* the decision. |
| **T2-3** | **Attempt + `session_secret` lifecycle** | `run_attempts.status ∈ {running, paused, wrapped, failed, superseded, lost}` (CHECK `0004_run_attempts.sql` (`run_attempts.status`)). `claim_epoch = wi.current_attempt_epoch + 1` in the claim transaction (`run_attempts.status`). A re-claim **INSERTs a new attempt**; the old row keeps `paused` forever (design `polyforge-v1-design.md` §3.2 *run_attempts.status*). Takeover sets the prior attempt to `superseded` (`internal/domain/run_attempts.go` (`FnClaimWorkItem`), (`FnForceTakeover`)) and emits `attempt_superseded` (`FnClaimWorkItem`). A replayed `idempotency_key` now reuses the recorded secret client-side (`internal/mcp/tools_lifecycle.go` (`registerLifecycleTools`)). | **LANDED** — `aihub#392.attrs.merged_2026_09_07` (merge `51108c9`, DB-gated, three arms with two green-on-both controls); `aihub#393.attrs.merged_2026_09_07` (merge `0f54228`) decided and enforced that `force_takeover` takes over the **work item**, never another work item's lock → 409 `CONFLICT_LOCK_TAKEN`. | 🔴 Three residues. (a) `'lost'` is in the CHECK and has **zero writers anywhere** — `git grep -In "'lost'"` over the whole repo returns exactly two hits, the CHECK itself (`0004_run_attempts.sql` (`run_attempts.status`)) and the design doc's copy of it (`polyforge-v1-design.md` §2.3 DDL, *run_attempts*); no Go, SQL, trigger or migration ever assigns it. Design **v1.24 `C-R9-5`** planned it ("zombie sweeper 改为 system force_takeover + lost 状态", `polyforge-v1-design.md` Changelog **v1.24**), and that sweeper does not exist: `grep -n 'SET status' internal/domain/gc.go` writes only `'archived'` (memories, `idx_locks_owner`) and `'queued'` (the unblock sweep, `idx_locks_owner`) — **no GC path assigns any `run_attempts.status` at all**. (b) No status means "its work item was cancelled", stated at `internal/domain/work_items.go` (`CancelWorkItem`), so a cancelled wi keeps an attempt marked `paused`. (c) `#392.attrs…findings_not_fixed`: one root cause, **two status codes** — `pf_update_step` answers **401 `UNAUTHORIZED`** and `pf_emit_event` **403 `ATTEMPT_MISMATCH`** for the same invalid secret, so a caller classifying by code sees two problems. | `pf_claim_work_item` 230, `pf_complete_attempt` 185, `pf_pause_attempt` 29, `pf_force_takeover` 1 (§0.1). | **One status code for "invalid attempt credential" across all tools; retire `'lost'` or give it a writer; add the cancelled-attempt status the cancel path says it needs.** A dead enum value and a duplicated meaning both cost nothing to carry and mislead every reader who assumes the vocabulary is real. |
| **T2-4** | **`heartbeat` — the word "lease"** | 🔴 There is no lease. `pf_renew_lease` answers **410 Gone**, "claim is permanent ownership" (`internal/server/routes_step.go` (`handleRenewLease`)); `expires_at` was removed in v1.21 (`docs/design/polyforge-v1-design.md` Changelog **v1.21**). The schema still says *"Send a heartbeat ping to keep the **lease** alive (resets `step_started_at`)"* (`internal/mcp/tools_step.go` (`registerStepTools`)). The handler is a **best-effort** bump whose error is discarded — `_, _ = pool.Exec(...)` at `internal/server/routes_step.go` (`handleUpdateStep`) — and it **returns early**, so any `step_id`/`status` sent alongside is silently dropped. | **DECIDED, QUEUED** — `aihub#398.attrs.owner_decision_2026_09_07.also_in_scope`: rewrite the sentence to say it resets `step_started_at` only, and state that the heartbeat branch discards `step_id`/`status`. That sub-item rides on `#398`, which is `running` — but the decision is recorded, so treat it as **LANDING** alongside T2-2. | 🔴 The MCP layer already refuses `next_step` + `heartbeat=true` with a reason — "a heartbeat only refreshes the lease and completes no step" (`internal/mcp/tools_step.go` (`validateNextStepArgs`)) — so the *guard* against the confusing combination exists while the *word* that causes the confusion stays. And `_common/lifecycle.md` teaches `heartbeat=true` every ~5 min, i.e. the skill text is the main producer. | `pf_update_step` 281 calls (§0.1); share with `heartbeat=true`: **not measured**. | **Take `#398`'s wording as decided, and make the heartbeat's DB error observable rather than discarded.** A best-effort write behind a name that promises a lease is a guard that can fail without anybody learning; if nothing depends on it, say so — if something does, check the error. |
| **T2-5** | **Event vocabulary** | 🔴 There is **no** `event_type` vocabulary. `agent_events.event_type` is `TEXT NOT NULL` with **no CHECK** (`internal/db/migrations/0006_events_memories.sql` (`agent_events.event_type`)), and `internal/domain/memory.go` (`EmitEvent`) validates only payload size (64 KB), `adminOnlyEventTypes` and, when `admin:true`, `adminEventWhitelist` (7 entries). Any other string is accepted. The one CHECK that *looks* like a whitelist is a **NULL-`work_item_id`** whitelist of 21 types (`0026_memories_latest_id.sql` (`agent_events.chk_evt_work_item_id`)) — it governs which events may omit a wi, not which events exist. | **PARTLY DECIDED.** The admin sets have a citation (`internal/domain/memory.go` (`adminEventWhitelist`), design §5.2 H10) and the forgery guard has one ((`adminOnlyEventTypes`), H6). **NO DECISION EVIDENCE FOUND** that `event_type` should be open, or that it should be closed. `aihub#343` added `lock_acquired`/`lock_released`/`wi_resources_updated` **with no backfill**, a caveat the tool description carries (`internal/mcp/tools_events.go` (`registerEventTools`)). | 🔴 Three overlapping-but-different sets over one column and **membership in one implies nothing about the others**: `adminOnlyEventTypes` = 4 (`memory.go` (`adminOnlyEventTypes`): `admin_redact, admin_unblock, admin_force_takeover, admin_gc_manual`), `adminEventWhitelist` = 7 (`adminEventWhitelist`), the NULL-wi CHECK = 21. `admin_gc_manual` is in the first and **not** the second, which inverts the flag: an admin sending it with `admin:true` is refused 403 "not in the admin whitelist" (`EmitEvent`), while the **same** admin omitting the flag succeeds. And `pf_read_events`' `types` param is described as a "whitelist" when it is a **filter** — a caller can filter on a type that can never exist. | `pf_emit_event` **118 calls** + `pf_read_events` 26 (§0.1). How many distinct `event_type` strings are actually in flight: **not measured** (needs a DB read, out of scope). | **Publish the event vocabulary as an enum on `pf_emit_event` and rename the `types` filter's description.** Free-text on a write path plus three partial whitelists means the "vocabulary" is whatever anybody has ever typed, and a typo is indistinguishable from a new kind of event. |
| **T2-6** | **Memory types** | 🔴 **Four vocabularies for one column.** DB: no CHECK on `memories.type` at all (`0006_events_memories.sql` (`memories.type`) — `type TEXT NOT NULL`). Go: **4-prefix** check plus a `'\|'` ban (`internal/domain/memory.go` (`Remember`)) — deliberately lenient. MCP: a **13-value** `propEnum` on `pf_remember` (`internal/mcp/tools_memory.go` (`rememberSchema`), from `domain.PfRememberTypeEnum`). UI select list: **19** (`domain.MemoryTypeEnum`, `internal/domain/memory.go` (`MemoryTypeEnum`)), described in its own comment as "Select-UX list ONLY — server validation stays lenient". | **DECIDED and documented as intentional** — `aihub#70` for the curated list (`memory.go` (`MemoryTypeEnum`)), `aihub#210` for the `methodology.*` refusal on `pf_remember` (`tools_memory.go` (`validatePfRememberArgs`)), `aihub#289` for the `'\|'` ban on **both** paths, with the reason written out: guarding only the read side would have left "the two halves of one contract disagreeing" (`memory.go` (`Remember`)). | 🔴 The enum is **advisory only** — the SDK does not reject off-enum values, verified in-repo ("the wiring tests still pass while sending `\"Brief\"`/`\"BRIEF\"`/`\"\"`", `tools_memory.go` (`recallSchema`), about the sibling `fields` enum). So a caller that ignores the enum stores an off-list type with a valid prefix, which `MemoryTypeEnum` will never render and which no CHECK forbids. | `pf_remember` 87, `pf_recall` 173, `pf_activate_memory` 21, `pf_get_memory` 7 (§0.1). Off-enum type rate: **not measured**. | **Keep the leniency, but stop calling the 13-value list an enum in a schema the SDK will not enforce — and add the DB CHECK for the four prefixes.** Three of the four vocabularies are deliberate; the missing one is the DB, which is the only layer that can make an unrecallable row impossible. |
| **T2-7** | **Artifact states** | 🔴 There is no artifact state. `pf_adopt_artifact` / `pf_close_artifact` / `pf_ignore_artifact` are thin wrappers that emit **one event**, `artifact_action`, with `payload.action ∈ {adopt, close, ignore}` (`internal/mcp/tools_memory.go` (`registerMemoryTools`), body at (`buildArtifactActionBody`)). They share one `artifactActionSchema()` (`artifactActionSchema`). **`artifact_action` has zero readers in the live tree**: `git grep -In artifact_action` returns only the five write-side sites in `internal/mcp/tools_memory.go` ((`registerMemoryTools`), (`buildArtifactActionBody`)), one design-doc line (`docs/design/polyforge-v1-design.md` §5.2 *Core Tools*), and one hit in the **archived** `v0/` Python tree (`v0/tests/v3/test_app_schemas.py` (`test_artifact_action_request`)) which reads nothing on this server. No Go, template or plugin file consumes the event. | **DECIDED, and the decision is what produced this** — the design doc line records the change ("adopt/ignore/close 语义改为 `pf_emit_event(type='artifact_action', …)`"), i.e. three tools were kept as an API surface after their state machine was replaced by an event. **NO DECISION EVIDENCE FOUND** that anything should read it. | 🔴 The fourth artifact tool, `pf_save_artifact`, is real and writes a `methodology.*` memory. So one of four "artifact" tools has an implementation and three are write-only aliases for an unread event. | **Measured (§0.1): all three action tools had ZERO calls in the 21-day window**, against `pf_save_artifact` 93. `pf_resolve_commit` — the one tool that *does* act on annotations — also had 0. | **Retire the three action tools, or give `artifact_action` a reader.** Three published tools, three schemas in the resident prefix, zero calls and zero effect: this is pure per-request cost for an API that does nothing observable. |
| **T2-8** | 🔴🔴 **Project member roles — two disagreeing `roleLevel` ladders** | **Legal member roles are `viewer \| writer \| maintainer`**, validated on write (`internal/domain/projects.go` (`UpdateProject`)) and typed on the struct (`MemberInput`). Two independent ladders rank them: **`internal/server/middleware.go` (`roleLevel`)** — a map, `viewer:1, writer:2, **maintainer:3**`; **`internal/domain/projects.go` (`roleLevel`)** — a func, `viewer:1, writer:2, **owner:3**`, everything else **0**. So **`maintainer` scores 3 in one and 0 in the other**, and `owner` — which is `projects.owner_user_id`, a column, never a member role — has a rung in the domain ladder that no member can ever occupy. Consequence at `checkProjectAccess` (`projects.go` (`checkProjectAccess`), `minRole="viewer"` via `GetProject`): a **maintainer** member fails `roleLevel(m.Role) >= roleLevel("viewer")` and the branch **returns immediately** (`checkProjectAccess`), skipping levels 4–5 — so a maintainer of a `visible:true` project gets **less** access than an anonymous stranger. | **NO DECISION EVIDENCE FOUND.** No wi, no `owner_decision`, no commit message, no CHECK. Both ladders carry comments claiming they agree. | 🔴 **The comments assert the agreement that does not hold.** `internal/server/router.go` (`handleListWorkItems`) says *"checkProjectAccess gates on `roleLevel[role] >= roleLevel[min]` … Going through the same lookup removes the class without needing to know"* — it is **not** the same lookup. `internal/server/ui_handlers_wi.go` (`checkProjectAccessSoft`) says *"Through roleLevel, matching checkProjectAccess"* — same false claim. `internal/domain/dependencies.go` (`ListDependencies`) and (`ListDependencies`) say it too, and there it is **true** (same package, same func) — which is precisely why the two server-side sites read as safe. The gate is blind: `internal/domain/projects_test.go` (`TestRoleLevel`) asserts `{"owner",3}` — a value the vocabulary cannot produce — and **never tests `"maintainer"`**. | Blast radius, measured: `GET /v1/projects/:name` (`internal/server/router.go` → `domain.GetProject`) and `pf_list_dependencies`' cross-project shadowing (`dependencies.go` (`ListDependencies`) — a maintainer sees `accessible:false, id:"hidden"`). **`pf_list_dependencies` = 41 calls; `pf_list_projects` = 2** (§0.1). Whether any live row has `role:"maintainer"` is **not measured** — migration `0013_backfill_projects.sql` (`projects.members`) mapped `maintainer→writer` on backfill, so it can only arrive from a `members` write made *since*, and `pf_update_project` had 8 calls in the window. | **Delete the domain `roleLevel` func and make both packages read one ladder that contains exactly the three legal member roles.** Two ladders over one vocabulary is not a style question — one of them denies the role the rest of the repo treats as the *most* privileged member, and every comment that would have caught it asserts they match. |
| **T2-9** | **`requires_human_session` — three states** | A `*bool`, so **`true` / `false` / `NULL`** (`internal/domain/work_items.go` (`WorkItem`), (`CreateWorkItemRequest`), (`UpdateWorkItemRequest`)). `NULL` is a real third state with its own ready-queue segment: `unclassified[]` = `queued + no blocker + requires_human_session IS NULL` (`internal/domain/work_items.go` (`GetReadyQueue`)). The schema publishes it as a bare `boolean`, *"Whether this wi requires a human session"* (`internal/mcp/tools_lifecycle.go` (`workItemFieldProps`)), with no mention of omission. | **DECIDED at the DDL level** — design v1.20 made the DDL default `NULL` "fail-safe" and added the `unclassified[]` segment (`docs/design/polyforge-v1-design.md` Changelog v1.20). `docs/mcp-tools.md`, the `pf_list_work_items` row already calls `NULL` "a third state (*unclassified*)" and states that this key is **never** projected away, "because there null is a third state … rather than 'none'". **NO DECISION EVIDENCE FOUND** for leaving the schema silent — `aihub#397` measured exactly this and was **cancelled** (see T1-9). | 🔴 The mismatch that made the third state expensive: `ErrRequiresHumanSessionMismatch` was designed as the safeguard that forces a mis-classified wi to be corrected (design §8.4 / §17 / §27 KL3) and **never fired** — its only call site compared a value against itself, `x != x`, so the 409 was unreachable; the branch is gone and `internal/domain/errors.go` (`ErrRequiresHumanSessionMismatch`) says in full: *"The design doc still describes this 409 as a live safeguard … That mitigation does not exist and never fired. Do not build on it."* | `pf_create_work_item` 275 + `pf_update_work_item` 354 (§0.1). This very wi, `aihub#411`, was created with `requires_human_session: null` — so the state is reached in practice, not hypothetically. 🔴 **And it did not stay null.** A `pf_update_work_item` call from this wi's own executor that sent **only** `work_item_id` and `attrs_patch` moved it from `null` to `true`, persisted, with no field of that name anywhere in the request. **Scope of that observation: the LIVE server, which is older than `1ec3bdc`** (§3.4) — on `origin/main` the MCP handler forwards `args` verbatim minus `work_item_id`/`brief` (`registerLifecycleTools`) and `buildWorkItemUpdate` gates the column behind `if req.RequiresHumanSession != nil`, so **this document does not claim `origin/main` does it**. Mechanism unknown and deliberately not guessed; the observation is one-shot and was not reproduced, because reproducing it means writing to a work item to see whether a field changes. Settle it with the same `attrs_patch`-only update against a scratch wi whose `requires_human_session` is `NULL`, on a server built from `origin/main`. | **State the third state in the schema (`omit ⇒ unclassified, not dispatched`) and revive `#397` for it.** A two-valued published type over a three-valued column means the state a caller reaches *by doing nothing* is the one the contract does not mention. |
| **T2-10** | **Release channels** | 🔴 They do not exist server-side. `pf_cut_alpha` and `pf_promote` are fully wired at hops 1–2 — schemas, required params, state-file credential injection, client calls (`internal/mcp/tools_release.go` (`registerReleaseTools`)) — and hop 3 returns **405 `NOT_IMPLEMENTED`**: `handleCutAlpha` → `"pf_cut_alpha: Phase 2"`, `handlePromote` → `"pf_promote: Phase 2"` (`internal/server/routes_step.go` (`handleCutAlpha`)). | **DECIDED as deferred** — design §5.6 "Release Tools（2 个）（H-R3-1 補充）" plus §5.8 "Scenario Testing（Phase 2 延后）"; the handler strings name Phase 2 explicitly. **NO DECISION EVIDENCE FOUND** for publishing them meanwhile. | 🔴 `pf_cut_alpha`'s description says *"Admin/**release-manager** only"* — and `release-manager` is **not a role anywhere**: `users.role ∈ {human-typed writer, admin}` (`0001_initial.sql` (`users.role`)) and member roles are `viewer\|writer\|maintainer` (T2-8). So the tool advertises a permission model that has no implementation and no vocabulary entry. | **Measured (§0.1): zero calls for both, in 21 days.** | **Unpublish both until Phase 2, per `#387`'s Plan B.** This is the exact shape `#387` ruled on — a published surface with nothing behind it — and it costs schema bytes in every request's prefix to advertise a 405. |
| **T2-11** | **Work-item identity: `id` / `slug` / `seq`** | `id` is `wi_` + 8 base62, `TEXT PRIMARY KEY`; `slug` is `GENERATED ALWAYS AS (project \|\| '#' \|\| seq) STORED` with `CREATE UNIQUE INDEX idx_wi_slug` (`0002_work_items.sql` (`work_items.slug`), (`idx_wi_slug`)); `seq` is unique **per project**, not per repo. Resolution is one predicate everywhere: `id = $1 OR slug = $1`, at 10 non-test sites (`work_items.go` (`resolveBlockedByRef`), (`GetWorkItem`), (`ResolveVisibleWorkItemRef`); `run_attempts.go` (`FnClaimWorkItem`), (`FnAcquireLocks`); `memory.go` (`resolveRememberWorkItemRef`), (`resolveRecallWorkItemRef`); `conflicts.go` (`PredictConflicts`); `commit_locks.go` (`FnReconcileCommitLocks`); `wi_vector.go` (`semanticQuerySource`)). Zero prefix dispatches. | **LANDED** — `aihub#402.attrs.merged_2026_09_07` (merge `1ec3bdc`) with the policy row stated: *"One id-or-slug resolution rule for the whole repo: `id = $1 OR slug = $1`, scoped to visible projects. No prefix dispatch anywhere."* Enforced structurally (`work_item_ref_policy_test.go`) and behaviourally (`work_item_ref_db_test.go`); `FormatIDOrSlug` deleted. Safety argued against migration 0002: a slug always contains `#`, an id never can, so at most one predicate is satisfiable (`#402.attrs…safety_of_the_widening`). | Two mechanisms for the "scoped to visible projects" half: `wi_vector.go` (`semanticQuerySource`) appends the scope **in SQL**, while `GetWorkItem` is unscoped and visibility is enforced **post-fetch** at the handler (`internal/server/router.go` (`handleGetWorkItem`), "C1: Require viewer access"). Both are correct; they are not the same shape. And `#402.attrs…decision_items_all_four` (3) left `/v1/work_items/ready` shadowing a wi whose slug is `ready` as a **documented note** (`internal/server/router.go`), on the ground that no slug can be `ready`. | `pf_get_work_item` 255 + every wi-scoped tool (§0.1). | **Nothing to rule on — record it as the settled policy and cite it in the contract cards.** One caveat worth writing down: "scoped to visible projects" is implemented two ways, so a new resolver must be told which one it inherits. |
| **T2-12** | **`declared_resources` types vs derived lock types** | Two closed sets that meet on one surface, with **one** overlapping value: `declared_resources[].type ∈ {repo, path, document, section, service, external_ref}` (`internal/domain/declared_resources.go` (`declaredResourceTypes`)) and `resource_locks.resource_type ∈ {git_branch, worktree, file_scope, tcp_port, deploy_env}` ((`resourceLockTypes`), mirroring CHECK `0004_run_attempts.sql` (`resource_locks.resource_type`)). Mapping: `repo→git_branch(<repo>/<branch\|main>)`, `path\|document\|section→file_scope`, `service→deploy_env`, `external_ref→` **no lock** (`resourceToLock`, `internal/domain/conflicts.go` (`resourceToLock`)). | **LANDED** — `aihub#238`: before it, an unknown declared type was skipped in silence, so a wi acquired no locks and `PredictConflicts` answered a **fake all-clear** (`declared_resources.go` (`declaredResourceTypes`)). Both sets and their validators now live in one file; `TestResourceLockTypesMatchMigrationCheckConstraint` parses the migration so the two copies cannot drift. | 🔴 `ValidateDeclaredResources` is **deliberately not** applied on stored-data paths: "roughly 14% of existing entries would fail it, and those work items must stay claimable" (`declared_resources.go` (`ValidateDeclaredResources`)). And only `FnClaimWorkItem` reports what it could not map; `FnForceTakeover` and `FnAcquireLocks` skip silently because their responses have no field for it — the comment closes with *"Do not read this comment as 'all three report'"* (`ValidateDeclaredResources`). | `pf_create_work_item` 275, `pf_update_work_item` 354, `pf_claim_work_item` 230, `pf_acquire_locks` 61, `pf_predict_conflicts` 66 (§0.1). Correct-shape rate: 🔴 relevant prior — a declaration of the wrong shape is **silent**, and this executor's own workspace memory records `{type:"path",uri:"file:…"}` as the correct form after getting it wrong. | **Add the unmappable-entry report to the other two responses, and publish the per-type `uri` scheme validation `#395` part (4) already decided.** The set mismatch is settled; what is not is that two of three lock-taking paths still fail silently, which is the exact defect `#238` closed on the third. |
| **T2-13** | **`file_scope` lock key shape** | `<project>:<repo>:<path>` when the declaration names a repo, `<project>:<path>` when it does not (`fileScopeLockKey`, `internal/domain/conflicts.go` (`fileScopeLockKey`)). Both segments were added to fix real hard-blocks: project (`aihub#222`) because a fork's byte-identical paths collided with its parent's, and repo (`aihub#261`) because repo-relative paths made every repo's `go.mod`/`Makefile`/`README.md` share one key — measured as a 409 between two wi's editing two **different** files. | **LANDED** — `aihub#222` (with migration `0028_file_scope_project_key.sql` rewriting every row) and `aihub#261` (no migration needed, because the empty-repo case reproduces the old key byte-for-byte — the compatibility argument is written out at `conflicts.go` (`fileScopeLockKey`)). The stranded-lock question was checked, not assumed: every release path keys on `owner_attempt_id`, never on `resource_key` (`fileScopeLockKey`). | The `requested_locks` param exposes the key shape to callers and its hint spells it out (`declared_resources.go` (`ValidateRequestedLocks`)), which is a second place the format is authored. | `pf_claim_work_item` 230, `pf_acquire_locks` 61 (§0.1); `requested_locks` is documented as "usually OMIT this". | **Nothing to rule on.** Record the two-shape key and the reason the empty-`repo` form must stay byte-identical, so nobody "tidies" it into a uniform three-segment key and strands live locks. |
| **T2-14** | **`step_attempt_id` required-ness** | Required for `completed`/`failed`; a nil value used to be accepted with the history row silently skipped, and every other `wi_step_completions` INSERT failure was swallowed inside `SAVEPOINT bp` while the call answered 200. Now refused up front (`validateTerminalStepArgs`, `internal/server/routes_step.go`; 3 `SAVEPOINT` statements remain, all the deliberate `escalated` one). | **LANDED** — `aihub#399.attrs.merged_2026_09_07` (merge `a5bab50`), CI verified at **step** level with three named `--- PASS` lines, and `#403` folded in as its (a) half. | 🔴 **`#399` shipped a deploy-order hazard and wrote it down**: once the server ships, a caller that omits `step_attempt_id` gets **400 where it used to get 200**, and a plugin binary older than the change forwards the key only when non-empty — so "bump the polyforge plugin **together with** the server deploy". Not mitigated on purpose: synthesising an id is the "record garbage rather than refuse" trade `#390` rejected. | `pf_update_step` 281 calls (§0.1); every `polyforge-coding` step template passes an id, so residual exposure is raw-HTTP and hand-rolled callers. | **Nothing to re-decide — but the hazard is an operational precondition, not a note.** Record "plugin bump and server deploy are one change" on the deploy checklist; a hazard that lives only in a wrapped wi's attrs is not a precondition anybody will check. |
| **T2-15** | **`force_takeover` scope** | Takes over the **work item**, never another work item's lock. A lock held by a running or paused attempt of a **different** wi answers **409 `CONFLICT_LOCK_TAKEN`** through the same cross-wi probe a normal claim uses; it *does* reclaim the target wi's own locks and rows whose owning attempt has ended. Two layers: `internal/domain/run_attempts.go` (`probeForeignLockHolders`) and a conditional `ON CONFLICT … WHERE NOT EXISTS (…)` backstop (`internal/domain/resource_events.go` (`lockUpsertSQL`), 5th bind param at (`acquireLockUpsert`)). | **LANDED** — `aihub#393.attrs.owner_decision_2026_09_07` ("No 'steal' flag is added") and `.merged_2026_09_07` (merge `0f54228`), with four mutants whose red sets are **disjoint**, proving both layers load-bearing. | 🔴 Two open residues, both filed as `aihub#410` (**queued**): `probeForeignLockHolders` reads **any** query error as "no conflict" — the conflict branch is gated on `if err == nil` (`run_attempts.go` (`probeForeignLockHolders`)), so a failed probe is indistinguishable from a clean one, degrading a `40001` in the SERIALIZABLE claim tx into an unclassified 500 instead of a retryable 409; and the schema sentence *"There is no flag that displaces another work item's lock"* overstates what `READ COMMITTED FnForceTakeover` guarantees — the commit-window race is documented at `resource_events.go` (`acquireLockUpsert`). | `pf_force_takeover` **1 call** in 21 days (§0.1) — but the same probe is on the `pf_claim_work_item` path (230). `#387.attrs.lock_incident` is the live instance: a `pf_ship` refused with 409 on a file held by `#393`, resolved by waiting, **not** by force-taking. | **Accept the landed semantics; rule on `#410`'s fail-open probe as a correctness bug, not a polish item.** A probe that reads *any* error as "no conflict" is fail-**open** on the guard `#393` exists to provide, so its own gate cannot see it fail. |
| **T2-16** | **Project visibility & the five-level access chain** | `projects.visible BOOL NOT NULL DEFAULT true` (`0012_projects.sql` (`projects.visible`)), plus a bcrypt `identifier`. Chain, in order (`internal/domain/projects.go` (`checkProjectAccess`)): admin → owner → member role ≥ `minRole` → `visible:true` grants **viewer** → correct `identifier` grants **viewer**. Levels 4–5 are skipped when `minRole` is `writer` or `owner`. A project outside an API key's `project_scope` is reported as **not-found**, not forbidden, so its existence is not revealed — and that applies to admins too (`checkProjectAccess`). | **LANDED** — the chain is documented in place; `aihub#377` decided that "you may not see this" has **one** wording that deliberately does not say which reason (`internal/server/middleware.go` (`notVisibleMessage`)), and that the old message must stop quoting the scoped project's name back at a non-member (`internal/server/router.go` (`handleListWorkItems`)). | See **T2-8**: level 3's early `return` on an insufficient role (`internal/domain/projects.go` (`checkProjectAccess`)) means a `maintainer` never reaches level 4, so this otherwise-sound chain inverts for exactly one legal role — a maintainer of a `visible:true` project is denied where an anonymous caller is admitted. | `pf_list_projects` 2, `pf_update_project` 8 (§0.1). | **Rule on T2-8 first; this chain needs no change of its own.** Worth recording that the not-found-for-out-of-scope rule applies to admins, since that is the kind of exemption a later "admins see everything" change would quietly remove. |
| **T2-17** | **User roles** | `users.role ∈ {writer, admin}` (CHECK `0001_initial.sql` (`users.role`)), `user_type ∈ {human, machine}` (`users.user_type`). `admin` bypasses every project check (`internal/domain/projects.go` (`checkProjectAccess`)) and, on the middleware path, is given an **empty** `ProjectRoles` on purpose (`internal/server/middleware.go` (`BearerAuth`), (`visibleProjects`)), which is why every `roleLevel` comparison carries an explicit admin exemption. | **LANDED** — design v1.4 (user system, permission matrix §3.4) and v1.11 (server-side enum validation). | 🔴 A third role name is published and does not exist: `release-manager` in `pf_cut_alpha`'s description (T2-10). Project roles are a **separate** vocabulary from `users.role` (T2-8) — different columns, different ladders. And the design doc still documents `PATCH /v1/admin/users` as validating `project_roles` values against `viewer\|writer\|maintainer` (`polyforge-v1-design.md` §4.3 *Users & Auth (Admin)*) on a column **dropped in migration `0014_drop_project_roles.sql` (`users.project_roles`)** — memberships live in `projects.members` now. | `pf_create_user` 1, `pf_update_user` **0**, `pf_list_users` 2, `pf_whoami` **0** (§0.1). | **Delete `release-manager` from the prose; keep the two-vocabulary split but name it in the contract cards.** "Role" currently refers to two columns with two ladders and one fictional value, and only the fictional one is visible to an LLM caller. |
| **T2-18** | **Reporter vs attempt owner vs watcher** | Three distinct identities. **Reporter** — `reporter_user_id` + a `reporter_display` snapshot, and the predicate that grants goal-change / reclassify / cancel (`internal/domain/work_items.go` (`UpdateWorkItem`), (`cancelGate`)). **Attempt owner** — `run_attempts.id` + `claim_epoch` + `session_secret_hash`, verified per mutating call (`internal/domain/memory.go` (`verifyAttemptCredentialSimple`)). **Watcher** — `wi_watches` (migration `0033`), whose FK violations are classified by SQLSTATE 23503 rather than by message (`internal/domain/wi_watches.go` (`pgForeignKeyViolation`), (`isForeignKeyViolation`)). | **PARTLY DECIDED.** The permission matrix is design §3.4; the display-name snapshots are v1.5. **NO DECISION EVIDENCE FOUND** for a `user_id` filter contract across tools — `pf_read_events` publishes `user_id` and `pf_list_work_items` publishes a user filter, but nothing states whether they mean reporter, actor or watcher. | 🔴 `cancelGate` grants cancel to `reporter \| maintainer \| admin` and its comment records why the reporter of a **blocked** wi must be able to cancel: it "has no other exit — `pf_claim_work_item` also rejects `status=blocked`", so such wi's were "permanently stuck" (`work_items.go` (`cancelGate`), `aihub#242`). That is the only place any of the three identities' *purpose* is written down. | `pf_read_events` 26, `pf_list_work_items` 77, `pf_cancel_work_item` 48 (§0.1). | **State on each `user_id`-shaped param which of the three identities it filters.** Three identities with one word ("user") on the wire is a set mismatch waiting to be discovered by a caller who filters events by reporter and gets actors. |
| **T2-19** | **Memory strength & the forgetting curve** | `effective = base_strength × exp(−days_since / stability_days)`, recomputed in SQL (`internal/domain/memory.go` (`recallText`)) and in Go (`MemoryStrength`). `stability_days` is **forced by a BEFORE INSERT trigger** from the type prefix, overriding whatever the caller sent: `rule.*` → immortal + 36500, `fact.*` → 180, `methodology.*` → 36500 with `expires_at` set at wrap (`0006_events_memories.sql` (`memories.is_immortal`), "User-supplied `is_immortal` value is overwritten by trigger"). `min_strength` default **0.3** (`internal/mcp/tools_memory.go` (`recallSchema`)). | **LANDED** — design v1.2 / v1.3 (`H-R3-10`/`H-R3-11` recomputed the dimensions and the worked example; `M7` set the 0.3 default; `H4` moved `methodology.*` to `expires_at`). | 🔴 The scale is incoherent with T1-3: `base_strength` is CHECK-enforced into **[1,5]**, so `min_strength=0.3` is below *every* legal base value — i.e. the default means "no filter", which is defensible, but the schema publishing `base_strength` as `(0-1)` makes 0.3 look like a mid-range threshold. Two fields, one scale, two published ranges. | `pf_recall` 173 (carries `min_strength`), `pf_remember` 87, `pf_reinforce_memory` 1 (§0.1). | **Fix T1-3's published range and then say on `min_strength` what scale it is on.** A threshold and the value it thresholds must be published on the same scale, or every caller's intuition about the number is wrong in the same direction. |
| **T2-20** | **Ready-queue segments** | The struct has **seven** keys — `items, running, stalled, paused, needs_human_session, unclassified, stale_running` — the last `omitempty`, so a caller sees six until it is non-empty (`internal/domain/work_items.go` (`ReadyQueue`)). The tool description says **"LCRS (6-section)"** (`polyforge dump-mcp-schemas`, `pf_get_ready_queue`). `ReadyItem.UnblockedAt` (`ReadyItem`) has **no writer anywhere**. `created_at` is set for `needs_human_session` and `unclassified` but not selected for `items` (`ReadyItem.CreatedAt` is `omitempty`). | **NO DECISION EVIDENCE FOUND** for any of the four. All were measured in `aihub#401`, which was **cancelled** on 2026-09-07 with a `correction_2026_09_07` attr confirming the `created_at` asymmetry is *as designed* per the design doc — but leaving the six-vs-seven, the dead `unblocked_at` and the undisclosed clamp unfiled. | 🔴 The design doc adds a third count and **three** dead fields: `polyforge-v1-design.md` §4.3 *Ready Queue (Layer 3)* draws **six** keys (no `stale_running`), promises `items[].unblocked_at`, draws `running[].expires_at` — removed in **v1.21** — and draws `kind` on all three item segments (`items[]`, `needs_human_session[]`, `unclassified[]`), a column **v1.22 deleted** ((`WorkItem`); verified: no `Kind` field anywhere in `internal/domain/work_items.go`). So the schema says 6, the struct has 7, and the design doc draws 6 with three fields that cannot exist. | `pf_get_ready_queue` 29 calls (§0.1). This is the tool `aihub#186`'s orchestrator design is built on, and `#387` already withdrew one dead param from it. | **Say "6 + `stale_running`" (or drop `omitempty`), delete `unblocked_at`, and revive `#401`.** `#387` proved the pattern on this exact tool: a to-be-built feature designed on top of a field that has no writer is how `#186` got planned against a no-op. |
| **T2-21** | **Step graph source & `scenario_ref`** | `wi_step_state.graph_source ∈ {scenario_config, scenario_default}` (CHECK `0005_step_state.sql` (`wi_step_state.graph_source`)), and the scenario SHA is pinned at claim (`scenario_ref`, migration `0018`). The `pf_get_step` description states flatly that there is **no** step graph in the response — "that is the scenario template". `completed_steps` is `[]` when nothing completed and **absent** only on a server older than `aihub#265` — "not the same answer". | **LANDED** — `aihub#265` removed the step-graph promise from the schema; `scenario_phase_configs` was dropped in migration `0017`. | 🔴 `docs/mcp-tools.md`, the `pf_get_step` row still promises *"Current step graph, status, progress, and previous steps"* — the sentence `#265` removed from the schema. Measured and filed in `aihub#400`, **cancelled**. Also from `#400`: the description tells a resuming agent to "treat every `step_id` in `completed_steps` as done" while `completedStepsQuery` has **no status filter** and `fnForceTerminateStep` writes `status='failed', error_type='force_terminate'` rows — each entry *does* carry `status`, so "the sentence tells the reader to ignore it". | `pf_get_step` 82 calls (§0.1); every resuming agent calls it first by contract. | **Revive `#400`.** Its findings are measured, uncontested and about the one call every resuming agent is told to make first — and cancelling it made them reachable only by knowing a cancelled wi's number. |
| **T2-22** | **`resources_version` / `members_version` — the two published CAS fields** | The only two version-shaped params in the whole published surface (verified against the dump: `pf_update_work_item.resources_version`, `pf_update_project.members_version` — no other param contains "version"). `members` is a whole-list **REPLACE**, so `members_version` alone cannot see a truncating caller — hence **412 `PROJECT_MEMBERS_UNDECLARED_REMOVAL`** with `expected_removals`, chosen over 409 precisely because clients loop on 409 and retrying re-sends the same short list forever (`internal/domain/errors.go` (`ErrProjectMembersUndeclaredRemoval`), `aihub#333` / `#260`). | **LANDED** — `aihub#260` (the version), `aihub#333` (the 412 and its reasoning, written out in the error code's own comment). | 🔴 The **unpublished** third one is the problem: `expected_version` on `pf_update_step` is not in the schema and never was bound, yet §0.2 measures it on **202 of ~1,120 calls (18%)** — and `#389.attrs.source_of_offenders_2026_09_07` established by `git grep` that **no** skill or fragment teaches it. `pf_update_step`'s own description now says so: "There is no version/CAS argument". | `pf_update_work_item` 354, `pf_update_project` 8, `pf_update_step` 281 (§0.1); the 18% figure is §0.2's frame, not this document's. | **Nothing to re-decide on the two real ones; the ruling belongs to T1-1.** Worth stating as its own row because the 18% is the strongest single number in this document and it is a *model-invented* CAS param on the tool that has explicitly refused one — no prose edit can remove it, only T1-1 phase 2. |

---

## 3. Cross-cutting observations the owner may want to rule on before the rows

Three patterns recur across the 34 rows. Each is a candidate policy in its own right.

1. 🔴 **"Cancelled as description-only" destroyed four wi's worth of measured findings.**
   `#397`, `#400`, `#401` were cancelled on 2026-09-07 with measured, cited, uncontested
   findings in their `content` — and no `owner_decision` and no cancellation reason in attrs.
   T1-9, T2-9, T2-20 and T2-21 exist because of them. Hop-1 prose is the *only* thing an LLM
   caller sees; a wrong description is a wrong contract.

2. 🔴 **"Folded rather than filed" has the same effect.** `#388`'s `findings_not_fixed` records
   that `recall_slim` "is STILL a keep-list and is instances 1-3 of this same pattern … folded
   rather than filed" — so T1-5's last keep-list has **no wi**. `#392` folded `#393`'s
   description sentence successfully, which is the counter-example: folding works when the
   receiving wi is live, and loses the finding when it is already wrapped.

3. 🔴 **Every gate that would have caught T2-8 asserted the invariant it was blind to.**
   `TestRoleLevel` tests a rung the vocabulary cannot produce (`"owner"`) and omits the one it
   can (`"maintainer"`); two comments claim "the same lookup" / "matching checkProjectAccess"
   about two different lookups. A comment asserting an invariant is a claim to be falsified,
   not evidence — and the cheapest way to find a false one here was the **honest neighbour**
   next to it (`dependencies.go` (`ListDependencies`) makes the same claim and is *true*, which is exactly
   why the false ones read as safe).

4. **Deployment reality, so no row is read as live.** Every merged wi in this chain carries
   `not_deployed_yet: TRUE` in its attrs (`#387`, `#388`, `#392`, `#393`, `#399`, `#402`).
   `origin/main` is ahead of production. Everything in the "Current behaviour" column is
   `1ec3bdc`, **not** what the live server at the configured aihub URL does — so on the live
   server `pf_get_ready_queue` still publishes `non_conflicting`, the claim response still
   strips `requires_human_session`, and a replayed `idempotency_key` still leaves the caller
   unauthenticated.

---

## 4. Appendix — candidates parked, with the reason each was cut

Name only, per the wi's ≤40-row cap (34 used). None of these is a non-finding; each lost on
*scope* or on *rank*, and the reason is stated so the cut can be reversed.

| candidate | why it did not make the cut |
|---|---|
| `force_reason` — unpublished ≥10-byte minimum (`internal/domain/work_items.go` (`CreateWorkItem`)), auto-defaulted client-side (`internal/mcp/tools_lifecycle.go` (`applyForceReasonDefault`)), never persisted | Single-tool residue (`pf_create_work_item`), explicitly out of scope; measured in `#397`. Note the shape though: three "reason" params share the ≥10 server minimum and only **one** publishes it — `reclassify_reason` (`internal/mcp/tools_lifecycle.go` (`registerLifecycleTools`)), while `goal_change_reason` (`registerLifecycleTools`) and `force_reason` (`workItemFieldProps`) do not. 🔴 And the default fires only when the value is **empty**, so the ≥ 10 check is unreachable for an omitted `force_reason` and reachable only for a caller that sends a non-empty string shorter than 10 — i.e. the validation guards a case the in-tree client cannot produce. |
| `409 CONFLICT_DUPLICATE details.existing.status` hardcoded `"active"` | Single-tool; already folded into `#396`'s decision. |
| `docs/mcp-tools.md`, the `pf_create_work_item` row — `force_create` "bypasses on a soft-conflict" while it skips `checkDedup` entirely | Doc row, single tool; measured in `#397`. |
| `pf_get_ready_queue.max` described as "ready section" while it limits all three | Single-tool wording; the *policy* half (undisclosed clamp) is T1-2(a). |
| Gate robustness: `rowserr` misses struct-field row loops; the `wi_` prefix scan matches only the literal `strings.HasPrefix` shape | Already filed as `aihub#409` (queued at the time; wrapped 2026-09-07) with both citations; a tier-1 row would restate the wi. |
| `escalated` + `status=completed` neither persisted nor rejected | `#398`'s `also_in_scope` covers the disclosure; behaviour change is explicitly not in scope there. |
| `pf_save_artifact` / `pf_update_step` 500 on a literal NUL byte in content | Two endpoints, one class; `#399.attrs…class_still_open` (2) and (4) already record both plus the reason the clean fix would destroy its own gate's only input-reachable fault. |
| The `escalated` blocked+`wi_stalled` savepoint's own invariant has **no gate** | `#399.attrs…class_still_open` (1) states it and why it was left; adding a row would not add information. |
| `worktree` and `tcp_port` lock types — legal in the `resource_locks` CHECK (`0004_run_attempts.sql` (`resource_locks.resource_type`)) and reachable **only** through client-supplied `requested_locks`, because `internal/domain/conflicts.go` (`resourceToLock`) produces just `git_branch`, `file_scope` and `deploy_env` | Real dead-vocabulary finding of the same shape as `'lost'` (T2-3a), but it is one more instance of a pattern T2-3 and T2-12 already carry; ranked out rather than scoped out. |
| The three `*_reason` params' shared, mostly-unpublished ≥10-byte minimum | Folded into the `force_reason` row above rather than given a row of its own; the citations are `internal/mcp/tools_lifecycle.go` (`registerLifecycleTools`) / (`registerLifecycleTools`) / (`workItemFieldProps`) against `internal/domain/work_items.go` (`UpdateWorkItem`) and (`CreateWorkItem`). |

---

## 5. Method, and what this document does not establish

- **Sources, all read-only.** `polyforge dump-mcp-schemas` (plugin `1.1.23`);
  `internal/db/migrations/*.sql`; `internal/domain/errors.go`;
  `pf_get_work_item` on `#387`, `#388`, `#389`, `#392`, `#393`, `#394`, `#395`, `#396`, `#398`,
  `#399`, `#402`, `#408`, `#409`, `#410` (attrs) and full `content` on the cancelled `#397`,
  `#400`, `#401`; `docs/audits/aihub-385-mcp-contract-audit-batch1.md`;
  `docs/design/polyforge-v1-design.md`; `git grep` over `internal/`, `cmd/`, `pkg/`, `docs/`.
- **No subagents were used** and no file outside this one was written. `git status --porcelain`
  on the wi branch shows exactly one entry, this file.
- 🔴 **Citation form: this document's first push was RED, and T1-13 is why.** The executor brief
  for `aihub#411` required a `file:line` on every behaviour cell. This repo **forbids that**, is
  gated on it (`scripts/pf_docs_contract_check.py` check C1, wired on `docs/**`), and has the
  measurement to back it up — 18 of 23 line citations in `docs/superpowers/` already pointed at
  the wrong line. The first push therefore produced **156 C1 errors** and a red Contract Lint on
  a doc-only PR. All **222** citations were converted to semantic anchors (file plus symbol);
  the `C1_ALLOWED` escape hatch was **not** touched, because the gate's own file says in so many
  words not to use it for a real citation.
  Fixing it surfaced two further defects **of the kind the fix was for**, both caught and both
  worth recording because an automated citation rewrite is exactly where false anchors get
  introduced: (i) bare continuation anchors (`` `:1614` ``) inherited the *wrong file* when a
  sentence changed subject, and (ii) a "comment attaches to the next declaration" rule misfired
  on comments **inside** a function body. Together those emitted symbols that were real
  declarations in real files and simply **not the code being described** — worse than a line
  number, and invisible to a check that only asks "does this symbol exist?". The fix was a
  span-aware resolver plus a verifier that asserts **the emitted symbol's declaration span
  contains the line the citation came from**: 222 symbols, 0 span failures. The CI script itself
  was then run locally (`--self-test` OK, full check OK) rather than a hand-written equivalent.
- **Column shape — one deliberate merge, disclosed.** The wi lists *current behaviour* and
  *code location(s)* as two columns; they are merged into one here, because the whole rule this
  wi imposes is that **every** behaviour claim carries its own citation. Split apart, a reader
  has to match claims to a list of locations by hand and a claim can lose its citation silently;
  merged, an uncited claim is visible on sight. Nothing was dropped — the other five columns are
  as specified.
- 🔴 **The `code_review` step of this wi was a PRODUCER SELF-REVIEW.**
  `common/review/SKILL.md` requires a clean-context subagent and calls self-review an invalid
  basis for advancing a wi; the executor mandate for this run forbids subagents, and that
  overrode it. It was made mechanical rather than impressionistic. At the time it ran this
  document still cited code by line (see T1-13 for why it no longer does), so every citation
  was extracted by regex and checked for file existence and line range — **39 distinct files,
  0 missing, 1 range past end-of-file** — and 18 load-bearing citations were content-checked
  against the cited line. **Five defects in this
  document were found and fixed that way**, and they are listed here rather than quietly
  corrected, because a review that reports nothing is indistinguishable from one that did not
  run: (1) a **misattributed citation** — `internal/domain/memory.go` (`ReplyCommit`) caps a commit *reply*
  body, not a memory body, which turned out to make T1-4 stronger once corrected (the same
  20000 gets three different treatments and `memories.content` gets none); (2) **two unscoped
  negatives** — "zero readers" / "zero producers" were true of the live Go tree but the archived
  `v0/` Python tree holds a hit for each, so both now carry their scope; (3) an **overreach** —
  "the `force_reason` minimum can never be reached" was false, the client default fires only on
  an empty value; (4) an **unverified attribution** — the `'lost'` state was credited to design
  v1.3 when it is v1.24 `C-R9-5`, and checking it produced the sharper fact that no GC path
  writes `run_attempts.status` at all; (5) **three unescaped `|` characters** inside a table
  cell, which would have collapsed T2-1 into 13 columns in the /ui viewer. An independent read
  of this document never happened.
- **Local gate on `1ec3bdc`, before and unaffected by this change** (the diff is one markdown
  file): `go build ./...` exit 0, `go vet ./...` exit 0, `go test ./... -count=1` → 15 packages
  `ok`, **0 FAIL**, run with an isolated `GOCACHE` and `GOWORK=off` so the workspace's other 40+
  repos cannot cross-talk into the build cache.
- 🔴 **Not established.** (a) No live probe was run, so no row asserts what the deployed server
  does — see §3.4. (b) No parameter-level *frequency* was measured: §0.1 counts calls per tool,
  not which params those calls carried, so every "share of calls carrying X" cell says
  **not measured** unless §0.2 supplies it with its own denominator. (c) `aihub#403` was read
  only through `#399.attrs.folded_in`; it is a cancelled duplicate whose content is `#399`'s
  (a) half. (d) Whether any live `projects.members` row holds `role:"maintainer"` — the
  question that decides T2-8's *current* impact as opposed to its reachability — needs a DB
  read this wi may not make. (e) The `requires_human_session` side effect recorded in T2-9 is an
  **observation on the live server**, not a reading of `1ec3bdc`; it is one-shot, unreproduced,
  and its mechanism is not diagnosed. It is written down because the alternative — dropping an
  unexplained write to the very noun that row is about — is worse than carrying it with its
  scope attached.
- **One deliberate deviation from the brief, stated rather than absorbed.** The brief described
  `#389` phase 1 as "landed or landing" and `#396` as "in flight"; both are `status:queued`
  with `current_attempt_id:null`. The rows record the queued status, because a decision nobody
  is executing and a decision that has shipped are not the same input to the owner.

---

## 6. Adjudication (owner, 2026-09-07)

**Authority.** The owner (xiaokang.w) adjudicated this table in-session on 2026-09-07 and
delegated the write-up (「按照你的建议继续做」). Unless a row below says otherwise, the ruling
**is** that row's own *Recommended ruling* cell, accepted as written. Four rows carry an
**explicit owner position that overrides the recommendation** and are marked 🔴 below:
**T1-3**, **T1-7**, and the de-locking group **T1-8 / T2-12 / T2-15** (plus **T2-4**), whose
authority is `aihub#416`'s `attrs.owner_ruling_2026_09_07`.

**🔴 Every accepted ruling is written back TWICE**, per this wi's
`attrs.adjudication_writeback_requirement_2026_09_07` — an owner-directed, non-negotiable
requirement added after the `#416` lease incident, where a v1.21 design decision was
unrecallable by both `pf_recall` and wi semantic search and the main thread consequently
re-proposed the thing that had been removed:

1. **here**, as this section, and
2. **as a `pf_remember` entry** — 13 memories, one per tier-1 row, plus 4 grouped tier-2
   memories. Their ids are in §6.3. A ruling that exists only in a document nobody re-reads
   is the same failure the requirement was written to stop.

**Disposition vocabulary.**

| tag | meaning |
|---|---|
| `rule-recorded` | the ruling is a policy statement; it lives in the memory named in §6.3 and needs no work item |
| `wi-filed:aihub#N` | the ruling implies a behaviour change; filed silently, with this row's evidence carried verbatim into the wi body |
| `superseded-by:aihub#416` | the question is dissolved rather than answered by the owner's de-locking ruling |
| `already-landed:aihub#N` | the ruling was executed between this table being written and being adjudicated |
| `landed:aihub#N` | the ruling has since been IMPLEMENTED, and the row records what landed and when. Added 2026-09-09 for `#416`; distinct from `already-landed`, which means "was already done when the owner ruled" — this one means "was done afterwards, because the owner ruled" |
| `landed-in-part:aihub#N` | 🔴 part of the row landed and part did not. It exists so a partial landing cannot be read as a whole one: the row must say which half is still open. T2-12 was its only instance — the advisory half landed with `#416`, the silent-report half on `FnForceTakeover` / `FnAcquireLocks` did not — and that half landed with `aihub#509` on 2026-09-09, so **no row carries this tag today**. It is kept defined rather than retired: the next partial landing needs it, and a vocabulary that only exists while it is in use is one nobody reaches for |

⚠️ **The "Decided?" column of §1 and §2 has rotted since it was written, and that is the
expected direction.** Seven wi's the rows describe as `queued` or `running` have since
**wrapped**: `#389`, `#395`, `#396`, `#398`, `#399`, `#408`, `#410`. And `#418` — which did not
exist when T1-5 was written — converted the last keep-list. The rows are left untouched because
they are the record of *what was reviewed*; this section is the record of *what was decided*.

### 6.1 Tier 1

| row | ruling | disposition |
|---|---|---|
| **T1-1** | Ship `#389` phase 1 as decided; the phase-2 trigger becomes a checked-in gate plus a re-runnable measurement, not a number in a wi's attrs. | `wi-filed:aihub#431` |
| **T1-2** | Rules 1 and 2 stand; widen the AST gate to hop 2 and give `ReadyQueue` the disclosure field — the gate is package-scoped while the policy is repo-scoped, so widen the gate, not the policy. | `wi-filed:aihub#432` |
| **T1-3** | 🔴 **Owner:** publish **1-5** — the DB's scale — and validate it in Go at the `Remember` entry point, reusing the reinforce clamp's bounds. | `wi-filed:aihub#433` |
| **T1-4** | Adopt `#396`'s policy row verbatim as repo policy and give it a gate that **enumerates DB CHECKs, not fields**; a per-field fix produces a fourth instance. | `wi-filed:aihub#434` |
| **T1-5** | Delete-list is the only projection shape. The last keep-list was converted after this table was written. | `already-landed:aihub#418` |
| **T1-6** | Validate `cursor` as RFC3339Nano at the handler and answer 400 — a 500 on a caller-supplied token sends the reader to the server logs. | `wi-filed:aihub#435` |
| **T1-7** | 🔴 **Owner: ENABLE the header.** Make the in-tree client send `Idempotency-Key` on every POST/PATCH — enabling is cheaper than demoting the design, because all requests already flow through one client. Do **not** retire the middleware. | `wi-filed:aihub#436` |
| **T1-8** | 🔴 **Owner:** dissolved. `git_branch` and `deploy_env` lock derivation retires outright, so "what should `read` mean for repo/service" no longer needs an answer. `file_scope` is unchanged. **LANDED `aihub#416` (2026-09-09):** `resourceToLock`'s `repo`/`service` arms return `("","")`; `derivedLock`'s `lockType == "file_scope"` condition is deliberately KEPT even though `file_scope` is now the only type it can see, so a future lock type must state its own intent rule rather than inherit this one. The open sentence the row quotes ("deciding it needs rule 2 changed in the same breath") was honoured literally — rule 2 changed in the same commit, and stopped reading the lock table. | `superseded-by:aihub#416` · `landed:aihub#416` |
| **T1-9** | Prose that contradicts hop 3 or 4 is a **bug at the same priority as the behaviour**, and "description-only" is **not** a cancellation reason. Legal dispositions are: withdraw the param, fix the code so the prose becomes true, or file it. The three cancelled wi's are revived below. | `rule-recorded` |
| **T1-10** | One budget over the whole `tools/list` payload, with per-tool ceilings derived from it; the resident cost is a property of the payload, not of a tool. | ~~`wi-filed:aihub#437`~~ → **`already-landed:aihub#419`** (merge `6db45a2`, PR #359). 🔴 The ruling was **already implemented when `#437` was filed**: `internal/mcp/tools_list_payload_budget_test.go` (`toolsListPayloadBudget`) budgets the whole serialised `tools/list` payload, and (`toolsListPerToolShareLimit`) derives the per-tool ceiling as a *share* of that total — the exact shape ruled here, measured through the real `tools/list` rather than `dump-mcp-schemas`. `#437` was filed ~6h after `#419` wrapped and **cancelled 2026-09-08 with no reason recorded anywhere** (its attrs are empty), which left `aihub#481` unable to separate "the class was deliberately rejected" from "closed for an unrelated reason, so the question survives" — it recorded that the class decision "must be re-confirmed with the owner before any work" and set `requires_human_session` rather than guess. Root cause was mutual invisibility — neither the gate nor this row named the other; the gate's header now points back at this row (`aihub#488`), so **keep both halves in step**. **Update 2026-09-11 (`aihub#554`): `aihub#481` was itself cancelled on 2026-09-08 with empty attrs — a second no-reason-recorded closure in this same line of descent — and a semantic search over aihub work items on 2026-09-11 finds no successor carrying the per-property byte-cost question, so the class decision this cell says must be re-confirmed currently has NO carrier.** |
| **T1-11** | Never pre-declare a ratchet file — let the commit-time lock gate take it — and write that rule into the two files' own headers, where an executor will read it. | `wi-filed:aihub#438` |
| **T1-12** | Keep `request_adjusted` exactly as decided and close the one `ReadyQueue` exemption. The absent-key shape is acceptable only while the field asserts nothing; do not let absence grow a meaning. **SECOND EXEMPTION RECORDED — bookkeeping, not a landing — `aihub#506` (owner ruling 2026-09-09, merge `0be8bbc`, PR #426):** `pf_reinforce_memory`'s saturation clamp is a second clamp that appends nothing to `request_adjusted`, so §1's "appended to by every clamp" now has two exceptions on record. It bounds stored strength plus `strength_delta` into `internal/domain/memory.go` (`MinBaseStrength`) and (`MaxBaseStrength`) at `internal/server/routes_memory.go` (`handleReinforceMemory`), and answers 200 carrying `memory_id` / `activation_count` / `base_strength` and no disclosure key. The disclosing option is precisely the one the owner declined: `#506`'s `attrs.owner_ruling_2026_09_09` reads "Option 1: KEEP the clamp, write it into the contract … No 400, no response-shape change", and `docs/mcp-cards/pf_reinforce_memory.md` records the two losing candidates as a 400 on an overflowing sum and a `clamped: true` field in the response. Disclosure therefore sits at hop 1, in `strength_delta`'s own description, held there by `internal/mcp/tools_memory_test.go` (`TestPublishedStrengthDeltaSaysWhatReinforceEnforces`) rather than by this convention. 🔴 **Two exemptions are on record, only one is live, and it is not the one this ruling names.** `ReadyQueue`'s was structural and provisional — no field existed and it was stated as a known gap rather than fixed — and it CLOSED with `aihub#432` on 2026-09-08 (merge `102655f`, PR #379), where `internal/domain/work_items.go` (`newReadyQueue`) discloses `max`. The one that remains is neither: not a gap awaiting closure but an accepted divergence, and not structural either, since the reinforce body is an inline map with no struct to extend — what stands in the way is a contract decision, not a missing field. Recorded because this ruling's own reason outlives it: the convention's reach is now "every clamp except one an owner exempted", and no caller can read that exemption off a response that never carried the key. Per `aihub#521` this is bookkeeping of a ruling already made — §1 and §2 are untouched, no disposition tag changes, and whether the convention should name its own exemptions in `internal/domain/request_adjusted.go` (`RequestAdjustment`) is an owner call this row does not take. **THIRD EXEMPTION RECORDED, AND THE CONVENTION'S REACH IS NOW GATED — `aihub#551` (owner ruling 2026-09-09; the gate itself landed as `aihub#532`, PR #449, merge `ce6c300`):** `aihub#532` built the reach this row could never record, because the reach was a sentence — `internal/citest/clampdisclosure` enumerates every clamp in the tree and requires each one to disclose through `request_adjusted` or carry a ledger entry. Its FIRST run found a third undisclosed site nobody had ruled on: `internal/domain/memory_vector.go` (`RecallWithVector`) re-clamped `top_k` to 200 after `internal/domain/memory.go` (`normalizeRecallTopK`) had already bounded it and `Recall` had already disclosed it. `aihub#532` registered it as `pending-adjudication` rather than waiving it quietly, and the owner ruled it **DELETED** (`aihub#551` `attrs.owner_ruling_2026_09_09`, 2026-09-09). The clamp, its ledger entry, and the arm that pinned it as unadjudicated are gone. 🔴 **The deletion did NOT make `normalizeRecallTopK`'s “ONLY place” sentence true — which is exactly what the ruling's own rationale predicted it would do.** Executing `aihub#551` measured a further bounding site that the census had enumerated but that nothing had ever connected to that sentence: `internal/server/queryparam.go` (`queryIntLenientUI`), which `internal/server/ui_handlers_memory.go` (`handleUIMemories`) calls with a ceiling of 200, and whose result becomes `RecallRequest.TopK` before `domain.Recall` is ever reached. It bounds a recall page size UPSTREAM of the normalizer — the precise direction that sentence names as the `aihub#309` hazard — and is invisible from there. It is harmless today only by a coincidence of literals: its ceiling is spelled 200, the same number as `recallTopKCeiling`, so raising that constant would leave `/ui` silently capped at the old value. The sentence was therefore **rewritten to the measured two-site truth** rather than declared true — this row's wi carried “rewrite the sentence” as the standing alternative to “make it true” — and rewritten in BOTH places that asserted it, since `internal/server/routes_memory.go` (`recallContentMax`) carried a second copy of the same false claim (“bounded in exactly one place”). **Corrected exemption accounting.** The paragraph above says two exemptions are on record and one is live. **Three** are on record and **two** are live: `ReadyQueue`'s (structural, provisional, CLOSED with `aihub#432` on 2026-09-08), `pf_reinforce_memory`'s saturation (accepted-contract, live, `aihub#506`), and `queryIntLenientUI`'s (**structural, live, and never written down until now**). The third one is structural for a reason no field can fix: `/ui` answers in server-rendered HTML, so there is no `request_adjusted` key to disclose through. It was carried in code by `aihub#532` and is recorded in this table for the first time here; its own identifier `n` evades the gate's published-parameter vocabulary check, a hole `aihub#532` stated in the scanner rather than papered over. Note also that a waiver key omits the line number and the bound, so the reinforce saturation is TWO clamp sites under ONE ledger entry — entry counts and site counts legitimately differ. **SETTLED — `aihub#552`, owner ruling 2026-09-09 (option ③), landed the same day (PR #451, merge `2672f47`; wi wrapped 2026-09-09):** whether `/ui`'s ceiling and `recallTopKCeiling` should be unified was ruled NO — the fork stays, and the coincidence of literals stops being a coincidence by record rather than by unification. What landed is the option's two halves exactly: mutual-pointer comments at both sites stating the two 200s are deliberately equal and naming each other, plus one text-level gate, `internal/server/ui_recall_ceiling_gate_test.go` (`TestUIRecallLimitCeilingEqualsRecallTopKCeiling`), that reads BOTH literals out of the source and fails when either side moves alone. No behaviour change; the constants stay unexported, so the gate never uses the constant as an oracle — the recorded no-expectations-from-constants decision stands — and the clampdisclosure structural waiver for `queryIntLenientUI` is kept, since voiding it belonged to the rejected option ② (deleting `/ui`'s own cap). The third exemption in the accounting above therefore stays live and structural, now with a gate holding its value to `recallTopKCeiling`'s. (Row updated 2026-09-11, `aihub#554`.) Per `aihub#521` this append is bookkeeping plus one correction of this row's own prose: §1 and §2 are untouched, and no disposition tag changes. | `wi-filed:aihub#432` |
| **T1-13** | Nothing to re-decide about C1 itself: add `md` to its extension set and state the citation form in the executor-facing docs. The filename-less single anchor stays with `aihub#406`; do not re-decide it. | `wi-filed:aihub#439` |

### 6.2 Tier 2

| row | ruling | disposition |
|---|---|---|
| **T2-1** | One editability matrix for the whole struct, one error code per rejection **kind** (409 state, 403 permission), and no field silently exempt. | `wi-filed:aihub#440` |
| **T2-2** | Accept `#398` as scoped — identity predicate now, state predicate held behind a corpus re-measure. The sequencing **is** the decision; do not re-open. | `already-landed:aihub#398` |
| **T2-3** | One status code for "invalid attempt credential" across all tools; retire `lost` or give it a writer; add the cancelled-attempt status the cancel path says it needs. | `wi-filed:aihub#441` |
| **T2-4** | 🔴 **Owner: the word "lease" stays dead.** The wording fix landed with `#398`; what remains is the discarded heartbeat DB error — check it, or state in code that nothing depends on it. **RE-AFFIRMED, not landed, by `aihub#416` (2026-09-09):** that wi replaced two exclusion mechanisms and introduced no expiry of any kind — the repo pin does not lapse, the service generation has no window, and `last_active_age_seconds` is published for a human with no code branching on it. Its design-doc entry (v1.25) states the prohibition explicitly. The heartbeat DB error `#442` names LANDED 2026-09-08 (PR #396, merge `fee1ca6`; `aihub#442` wrapped the same day): the "check it" arm was taken, because something does depend on the write — the bump now runs through `RETURNING` with its error checked, a failed statement answers 500 naming the failure instead of vanishing, every heartbeat response states `step_started_at_refreshed` rather than only the false case, and a `step_id`/`status` the early return drops is disclosed through `request_adjusted` for the direct-HTTP caller, the one hop that can still send them. (Row updated 2026-09-11, `aihub#554`.) | `superseded-by:aihub#416` · `wi-filed:aihub#442` |
| **T2-5** | Publish the event vocabulary as an enum on `pf_emit_event` and rename the `types` filter's description. Free text plus three partial whitelists means a typo is indistinguishable from a new kind of event. | `wi-filed:aihub#444` |
| **T2-6** | Keep the leniency; stop calling the 13-value list an enum in a schema the SDK will not enforce; add the DB CHECK for the four prefixes — the DB is the only layer that can make an unrecallable row impossible. | `wi-filed:aihub#445` |
| **T2-7** | Retire the three action tools, or give `artifact_action` a reader. Zero calls, zero readers, three schemas in every request's prefix. | `wi-filed:aihub#446` |
| **T2-8** | Delete the domain `roleLevel` func; both packages read **one** ladder containing exactly the three legal member roles, and the test must pin `maintainer` instead of the rung the vocabulary cannot produce. | `wi-filed:aihub#443` |
| **T2-9** | State the third state in the schema (omit means *unclassified, not dispatched*), and settle the one-shot live side effect with a **two-armed** reproduction, since one arm cannot distinguish "already fixed" from "never happened". | `wi-filed:aihub#447` |
| **T2-10** | Unpublish both release tools until Phase 2, per `#387`'s Plan B — and close `aihub#423` explicitly, since unpublishing removes its premise and a duplicate left open is one that rots. | `wi-filed:aihub#448` |
| **T2-11** | Settled policy, nothing to rule. Record the one caveat: "scoped to visible projects" has **two** implementation shapes, so a new resolver must be told which one it inherits. | `already-landed:aihub#402` · `rule-recorded` |
| **T2-12** | 🔴 **Owner:** repo and service entries become **advisory** and derive no lock. The surviving half — the unmappable-entry report on the two lock paths that stay silent — rides on `#416`'s spec, which already names this row. **LANDED `aihub#416` (2026-09-09)** for the advisory half: the mapping table this row prints is now `path` / `document` / `section` → `file_scope` and `repo` / `service` / `external_ref` → no lock, and `declaredResourceTypes`' own comment names all three lockless types instead of one. **The surviving half LANDED `aihub#509` (2026-09-09)**, which is why this row's tag names `#509` and not `#416`: `FnForceTakeover` and `FnAcquireLocks` now return `unrecognized_resources` from the same `UnrecognizedDeclaredResources` call `FnClaimWorkItem` uses, so all three stored-data lock paths report under one key and one wording. `#416` had made the half smaller without closing it — it changed which types map, not which paths report, and `repo`/`service` stopped being unmappable at all — so what `#509` closed is the narrower set that remains: a type outside the declared vocabulary, and a legal type with no `uri`. ⚠️ **Two boundaries are deliberate and are not this row's residue.** The report is computed from the RAW stored payload rather than the decoded slice, because the decoders are tolerant by design and a report derived from them would be silent on the payload that is most wrong; and `pf_acquire_locks` still ERRORS on an unparseable payload rather than reporting it, because that endpoint's contract is "tell me which locks I hold" while the other two must keep a work item recoverable. | `superseded-by:aihub#416` · `landed:aihub#509` |
| **T2-13** | Nothing to rule. Record **why** the empty-`repo` key form must stay byte-identical, so nobody tidies the two shapes into one three-segment key and strands every live lock. | `rule-recorded` |
| **T2-14** | Nothing to re-decide. The "plugin bump and server deploy are one change" hazard is an operational **precondition**, not a note in a wrapped wi's attrs. | `already-landed:aihub#399` · `wi-filed:aihub#438` |
| **T2-15** | 🔴 **Owner:** the landed semantics are accepted; the fail-open probe was fixed and the race qualifier is filed, and `deploy_env` and `git_branch` lock derivation retires under the de-locking ruling, shrinking this row to `file_scope`. **LANDED `aihub#416` (2026-09-09):** the row is now `file_scope`-only in practice — `probeForeignLockHolders` and `lockUpsertSQL` are byte-unchanged, and what shrank is the lock set they operate on. Migration `0038` additionally deleted the existing `git_branch`/`deploy_env` rows, each with a `lock_released` event carrying the new `cause=derivation_retired`; without that they would have become rows no declaration explains and no probe reaches. The two types stay in the `resource_locks` CHECK and remain reachable through `requested_locks` (owner ruling Q-3), so "shrinking to `file_scope`" describes derivation, not vocabulary. | `superseded-by:aihub#416` · `already-landed:aihub#410` · `landed:aihub#416` |
| **T2-16** | The chain needs no change of its own — it inverts for exactly one legal role, so rule T2-8 first. Record that the out-of-scope-reads-as-not-found rule applies to **admins too**, since that is the exemption a later "admins see everything" change would quietly remove. | `rule-recorded` |
| **T2-17** | Delete `release-manager` from the prose; keep the two-vocabulary split but **name** it in the contract cards, since only the fictional value is visible to an LLM caller. | `wi-filed:aihub#448` |
| **T2-18** | State on each `user_id`-shaped param which of the three identities — reporter, attempt owner, watcher — it filters. | `wi-filed:aihub#444` |
| **T2-19** | Fix T1-3's published range first, then say on `min_strength` what scale it is on. A threshold and the value it thresholds must be published on the same scale. | `wi-filed:aihub#433` |
| **T2-20** | Say "6 plus `stale_running`" (or drop `omitempty`), delete `unblocked_at`, and revive the cancelled `#401`. The `created_at` asymmetry is settled as designed — do not re-open it. | `wi-filed:aihub#449` |
| **T2-21** | Revive the cancelled `#400`: its findings are measured, uncontested, and about the one call every resuming agent is told to make first. | `wi-filed:aihub#450` |
| **T2-22** | Nothing to re-decide on the two real CAS fields. The model-invented third one is ruled by T1-1 — no prose edit can remove it. | `rule-recorded` |

**Counts over the 35 rows.** `wi-filed` **25** · `already-landed` **6** · `superseded-by:aihub#416`
**4** · `rule-recorded` **5** (four sole, one alongside `already-landed`) · `landed` **3** (added
2026-09-09, all three on the `#416` group: T1-8 and T2-15 landed whole with `#416`; T2-12's
advisory half landed with `#416` and its reporting half with `aihub#509` on 2026-09-09, which is
why its tag names `#509`). The `landed-in-part` tag defined above is carried by **no** row and so
has no entry here — T2-12 was its only instance and closed on 2026-09-09. Rows carrying two tags
are counted under each, and a tag a row records as RETRACTED still counts where the row keeps it —
T1-10's struck-through `wi-filed` is inside the 25, beside the `already-landed` that replaced it.
**All 35 rulings** are additionally written back as memory, per the requirement above — the memory
write-back is universal, not an alternative disposition. These four counts, the row count, and the
filed-wi range below are recounted from the rows on every docs PR by check C4 in
`scripts/pf_docs_contract_check.py`; that gate exists because `aihub#488` edited a row and left the
tally behind it with nothing going red (`aihub#489`). Keep the two halves in step, as T1-10 says.

**Work items filed by this adjudication: `aihub#431` … `aihub#450`** (20). Each carries its
row's evidence verbatim, so a finding stays reachable without knowing this document's name.

### 6.3 Memory write-back index

One memory per tier-1 row; tier 2 grouped by theme. All `project: aihub`, `visibility: team`,
linked to this wi. None of them sends `base_strength` — T1-3 is the row about that trap.

| scope | memory |
|---|---|
| T1-1 unknown params | `mem_CQu56DJx` |
| T1-2 numerics | `mem_s0RuVBa5` |
| T1-3 `base_strength` | `mem_ajzGuz78` |
| T1-4 CHECK vs Go 400 | `mem_73NRHSXA` |
| T1-5 projections | `mem_fbxBLQpE` |
| T1-6 bad cursor | `mem_kZvkxheT` |
| T1-7 idempotency | `mem_1Nmcni5D` |
| T1-8 `intent:read` scope | `mem_Ri59guu8` |
| T1-9 stale-prose disposition rule | `mem_NvW0HC8a` |
| T1-10 schema wire budget | `mem_HPUHZk8q` |
| T1-11 ratchet-file convention | `mem_ohPUa1E0` |
| T1-12 `request_adjusted` | `mem_hktgOcNa` |
| T1-13 docs citation form | `mem_LIEo65EV` |
| tier 2 — identity, status, step model (T2-1, T2-2, T2-11, T2-14, T2-21, T2-22) | `mem_d6xeDHye` |
| tier 2 — attempt, secret, locks (T2-3, T2-4, T2-12, T2-13, T2-15) | `mem_53nKHJES` |
| tier 2 — memory, artifact, events (T2-5, T2-6, T2-7, T2-19) | `mem_U69MmisM` |
| tier 2 — roles, visibility, rhs, ready queue (T2-8, T2-9, T2-10, T2-16, T2-17, T2-18, T2-20) | `mem_atx0WpqB` |

The locks group deliberately **points at** the pre-existing concurrency-philosophy memory rather
than restating it: that memory is the authority for v1.21's ownership-only model and for the
2026-09-07 de-locking ruling together, and duplicating it is how two copies start to disagree.

### 6.4 🔴 What this adjudication did NOT settle

Listed, not decided. Each is a question the row's recommendation does not answer, and none of
them is blocked on the owner reading this document.

1. **T2-8's current impact, as opposed to its reachability.** Whether any live
   `projects.members` row holds `role:"maintainer"` needs a DB read. Migration `0013` mapped
   `maintainer` to `writer` on backfill, so such a row can only have arrived from a later
   members write. The fix is right either way; the *urgency* is unmeasured. Carried into
   `aihub#443`. **Settled 2026-09-08: `aihub#443` made the read and shipped the one-ladder fix
   (PR #376, merge `52e1263`).** Its `premise_check_443` attrs record FOUR live
   `role:"maintainer"` rows, and exactly the three legal role strings across all ten projects'
   47 member rows. The `GET /v1/projects/:name` half had ZERO live instances — each of the four
   rows belongs to the project's own `owner_user_id`, so the owner level short-circuits first —
   while the `pf_list_dependencies` half was live-reachable for one non-admin maintainer.
2. **T2-9's live side effect has no mechanism.** One-shot, unreproduced, observed on a server
   older than this document's baseline. It is carried with its scope attached rather than
   dropped, and settling it needs two arms — live build and `origin/main` build — or the result
   cannot tell "already fixed" from "never happened". Carried into `aihub#447`. **Settled
   2026-09-08: `aihub#447` ran exactly that two-armed reproduction (PR #407, merge `538c0d8`),
   and the update path was innocent.** On both arms the `attrs_patch`-only update left the
   column NULL and the CLAIM set it `true` — `FnClaimWorkItem`'s C-R9-12 server-default
   fallback, emitting `wi_classification_resolved` — and `aihub#411`'s own timeline carries that
   same ordered pair 51 minutes BEFORE the update the write was blamed on. The write is real,
   has a named live mechanism, and is not on the update path; nothing was fixed because nothing
   there was broken.
3. **T2-7 cannot prove there is no consumer.** Zero calls in one 21-day window is evidence of
   disuse; the /ui annotation flow uses the same adopt/close/ignore vocabulary and was not
   checked. Retiring the tools needs that checked first, not assumed. Carried into `aihub#446`.
   **Settled 2026-09-08: `aihub#446` checked, then retired (PR #413, merge `d77ccda`).** The /ui
   annotation flow does not use this vocabulary — reviewer annotations are memory commits with
   status open/resolved — so the three tools were unpublished; the `artifact_action` event
   itself stays accepted and historical rows keep rendering.
4. **T2-6's migration is unsized.** Adding a CHECK to a populated table fails on any existing
   off-prefix row, and the live distinct `type` set was never read. Carried into `aihub#445`.
   **Settled 2026-09-08: `aihub#445` (PR #414, merge `f128b69`) removed the need for the census
   rather than making it.** Migration `0034` adds the CHECK `NOT VALID` — which still enforces
   every new write — then counts the pre-existing rows in the same migration, runs
   `VALIDATE CONSTRAINT` when there are none, and otherwise records every offending type and row
   count in the constraint's own COMMENT. The live distinct `type` set remains unread from a
   development machine, and no longer needs to be.
5. **T2-5 has no census.** How many distinct `event_type` strings are actually in flight needs
   a DB read this wi could not make, so the cost of closing the vocabulary is unknown.
6. **The de-locking group's open questions are `#416`'s, not this table's.** The generation
   probe registry, where an invalidation is recorded, and what `pf_predict_conflicts` reports
   for an advisory entry were all listed in that wi's `spec_must_answer` and were open by design.
   🔴 **Two of the three are now answered (`aihub#416` landed 2026-09-09), one moved rather than
   closed.** `pf_predict_conflicts` reports `soft_block` for a declared repo (rewritten rule 2)
   and `info` for a declared service (new rule 6), both from a declaration join, both carrying
   `last_active_age_seconds`; invalidation is recorded in three places, of which two are
   load-bearing — the observation artifact's `structured_payload` and a `step` transition to
   `failed(error_type="observation_invalidated")` — with an `observation_invalidated` event as
   the derived third, and deliberately NOT in `work_items.attrs`. The probe REGISTRY moved to
   the per-project scenario repo (`services.yaml`), which is a different repository on its own
   lifecycle, so it is out of this table's reach rather than settled by it. And the ENFORCEMENT
   of the generation protocol is unbuilt on purpose (owner ruling Q-2 = C, 2026-09-08): the
   mechanism ships without a gate, because defining "an observation artifact" today would take
   a discriminator with no real samples behind it.
7. **T2-1 leaves one sub-question open in both directions.** "No field silently exempt" does not
   say whether `attrs` staying writable on a terminal wi is the defect or the feature — existing
   tooling depends on that write path. The wi states it rather than choosing.
8. **Nothing here describes the live server.** §3.4 still holds: every merged wi in this chain
   carries `not_deployed_yet`, and `origin/main` is ahead of production. An adjudicated ruling is
   not a deployed one.
